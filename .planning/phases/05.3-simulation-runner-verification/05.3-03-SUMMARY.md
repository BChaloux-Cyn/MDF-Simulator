---
phase: "05.3"
plan: "03"
subsystem: engine
tags: [bundle-loader, scenario-schema, preflight, pydantic, importlib]
dependency_graph:
  requires: [05.3-02]
  provides: [engine.ENGINE_VERSION, engine.bundle_loader, schema.scenario_schema, engine.preflight]
  affects: [05.3-04]
tech_stack:
  added: [importlib.util.spec_from_file_location, zipfile, tempfile]
  patterns: [TDD red-green, Pydantic v2 model_validator, engine isolation (no schema/ imports)]
key_files:
  created:
    - engine/bundle_loader.py
    - engine/preflight.py
    - schema/scenario_schema.py
    - tests/test_simulation_runner.py
    - tests/fixtures/minimal.scenario.yaml
  modified:
    - engine/__init__.py
decisions:
  - "engine/preflight.py uses Any type annotation (not schema.ScenarioDef import) to preserve D-37 engine isolation"
  - "preflight uses actual AssociationManifest field names mult_a_to_b/mult_b_to_a (not multiplicity_a/multiplicity_b as plan suggested)"
  - "BundleVersionError message includes both bundle engine_version and installed ENGINE_VERSION for clear diagnosis"
  - "Path traversal guard uses resolve().startswith(str(tmpdir_resolved)) per T-5.3-05"
  - "mdf_generated_* sys.modules entries cleaned up in autouse=False fixture to avoid Windows tmpdir cleanup failures"
metrics:
  duration: "~15 minutes"
  completed: "2026-04-10T18:34:06Z"
  tasks_completed: 2
  files_created: 5
  files_modified: 1
---

# Phase 05.3 Plan 03: Bundle Loader + Scenario Schema + Preflight Summary

**One-liner:** BundleLoader unpacks `.mdfbundle` zips via `importlib.util.spec_from_file_location`, verifies engine version, reverses `state::event` JSON keys to tuples, and rebinds compiled action/guard callables; `ScenarioDef` Pydantic v2 model validates scenario YAML with sender-required and mutual-exclusion constraints; `check_multiplicity` catches missing required-cardinality links before simulation.

## Tasks Completed

| # | Name | Commit | Key Files |
|---|------|--------|-----------|
| 1 | ENGINE_VERSION + BundleLoader | 183c4a6 | engine/__init__.py, engine/bundle_loader.py |
| 2 | Scenario schema + preflight | 95f7785 | schema/scenario_schema.py, engine/preflight.py |

Test scaffold committed at: cbfa305 (RED phase)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Fixed engine isolation violation in preflight.py**
- **Found during:** Task 2 GREEN verification (`test_engine_isolation_no_schema_imports` in test_engine.py)
- **Issue:** `engine/preflight.py` imported `from schema.scenario_schema import ScenarioDef`, violating the D-37 engine/* isolation constraint (engine/ must not import from schema/, tools/, or pycca/)
- **Fix:** Replaced `ScenarioDef` import with `from typing import Any`; `check_multiplicity` signature uses `Any` — duck-typed against `.instances` and `.relationships` attributes which is sufficient
- **Files modified:** engine/preflight.py
- **Commit:** 95f7785

**2. [Rule 1 - Bug] Preflight field name adaptation**
- **Found during:** Task 2 implementation (reading engine/manifest.py)
- **Issue:** Plan's preflight code used `multiplicity_a`/`multiplicity_b` field names; actual `AssociationManifest` TypedDict uses `mult_a_to_b`/`mult_b_to_a`
- **Fix:** Used correct field names throughout preflight.py; updated test fixture accordingly
- **Files modified:** engine/preflight.py, tests/test_simulation_runner.py

## Acceptance Criteria Verification

- `engine/__init__.py` contains `ENGINE_VERSION = "0.1.0"` — PASS
- `engine/bundle_loader.py` defines `def load_bundle`, `class BundleVersionError`, `class BundleCorruptError` — PASS
- `engine/bundle_loader.py` contains `importlib.util.spec_from_file_location` — PASS
- `engine/bundle_loader.py` contains `"::"` (key reversal) and `resolve()` (path traversal) — PASS
- `schema/scenario_schema.py` defines `class ScenarioDef`, `class InstanceDef`, `class EventDef`, `class TriggerDef` — PASS
- `schema/scenario_schema.py` contains `populate_by_name=True` — PASS
- `schema/scenario_schema.py` contains ≥3 `@model_validator` decorators — PASS (4)
- `engine/preflight.py` defines `def check_multiplicity` and `class PreflightIssue` — PASS
- All 11 bundle loader + 6 scenario/preflight tests pass — PASS (17 total)
- `pytest tests/test_engine.py -q` passes — PASS (43 passed)
- No new regressions (pre-existing failures in test_drawio_canonical.py and test_elevator.py are unrelated) — PASS

## Known Stubs

None. All tests exercise real implementations against the elevator bundle.

## Threat Flags

None. Mitigations T-5.3-04 (yaml.safe_load + Pydantic validation) and T-5.3-05 (path traversal guard) are implemented. T-5.3-06 (arbitrary code exec) accepted per plan. T-5.3-07 (version mismatch) mitigated by BundleVersionError.

## Self-Check: PASSED

Files created/exist:
- engine/bundle_loader.py: FOUND
- engine/preflight.py: FOUND
- schema/scenario_schema.py: FOUND
- tests/test_simulation_runner.py: FOUND
- tests/fixtures/minimal.scenario.yaml: FOUND

Commits exist:
- cbfa305 (RED tests): FOUND
- 183c4a6 (Task 1 GREEN): FOUND
- 95f7785 (Task 2 GREEN): FOUND
