---
phase: 05.3
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_simulation_runner.py
  - tests/test_simulation_e2e.py
  - tests/fixtures/minimal.scenario.yaml
  - tests/fixtures/minimal.mocks.yaml
  - .design/bundles/.gitkeep
  - .design/scenarios/.gitkeep
  - .design/traces/.gitkeep
autonomous: true
requirements: [MCP-08]
must_haves:
  truths:
    - "Test files exist with stub tests that fail for the right reason (NotImplementedError or import error)"
    - "Fixture scenario YAML exists and is parseable"
    - ".design/{bundles,scenarios,traces} directories exist"
  artifacts:
    - path: "tests/test_simulation_runner.py"
      provides: "Stubs for bundle loader, ctx API, scenario schema, preflight, MCP tool unit tests"
    - path: "tests/test_simulation_e2e.py"
      provides: "Stubs for elevator end-to-end and determinism"
    - path: "tests/fixtures/minimal.scenario.yaml"
      provides: "Minimal scenario fixture for loader tests"
  key_links:
    - from: "tests/test_simulation_runner.py"
      to: "engine.bundle_loader, engine.ctx, schema.scenario_schema, tools.simulation"
      via: "import statements inside test functions (lazy) so unimplemented modules don't crash collection"
      pattern: "import"
---

<objective>
Wave 0 test scaffold. Create test files with xfail/skip stubs for every verification target in 05.3-VALIDATION.md, plus fixtures and runtime directories used by subsequent plans. No implementation — just the contracts the later waves must satisfy.

Purpose: Unblock Nyquist sampling for all Phase 5.3 tasks.
Output: Failing-but-collectable test files, minimal scenario fixture, empty runtime dirs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.3-simulation-runner-verification/05.3-CONTEXT.md
@.planning/phases/05.3-simulation-runner-verification/05.3-RESEARCH.md
@.planning/phases/05.3-simulation-runner-verification/05.3-VALIDATION.md
@tests/test_engine.py
</context>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| test-fixture → YAML loader | Scenario fixture is YAML parsed by pyyaml in later plans |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5.3-01 | Tampering | YAML fixture load path | accept | Fixture is committed test data; consumers must use `yaml.safe_load` (enforced by T-5.1-01 project rule) |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Create test stub files and fixtures</name>
  <files>
    tests/test_simulation_runner.py,
    tests/test_simulation_e2e.py,
    tests/fixtures/minimal.scenario.yaml,
    tests/fixtures/minimal.mocks.yaml
  </files>
  <read_first>
    tests/test_engine.py,
    .planning/phases/05.3-simulation-runner-verification/05.3-VALIDATION.md,
    .planning/phases/05.3-simulation-runner-verification/05.3-RESEARCH.md
  </read_first>
  <action>
Create `tests/test_simulation_runner.py` containing exactly these stub test functions, each using `pytest.importorskip` or `pytest.xfail("Wave N pending")` so collection succeeds:

```python
import pytest

# Bundle loader tests (Plan 03)
def test_bundle_loader_extracts_and_verifies_version():
    pytest.xfail("Plan 03 not implemented")

def test_bundle_loader_hard_fails_on_version_mismatch():
    pytest.xfail("Plan 03 not implemented")

def test_bundle_loader_rebinds_transition_table_callables():
    pytest.xfail("Plan 03 not implemented")

def test_bundle_loader_reverses_state_event_keys():
    pytest.xfail("Plan 03 not implemented")

# ctx API tests (Plan 02)
def test_ctx_instance_key_populated_on_create_sync():
    pytest.xfail("Plan 02 not implemented")

def test_ctx_instance_key_populated_on_create_async():
    pytest.xfail("Plan 02 not implemented")

def test_ctx_generate_accepts_target_instance_key():
    pytest.xfail("Plan 02 not implemented")

def test_ctx_create_returns_instance_dict_with_keys():
    pytest.xfail("Plan 02 not implemented")

def test_ctx_delete_by_instance_dict():
    pytest.xfail("Plan 02 not implemented")

def test_ctx_relate_by_instance_dicts():
    pytest.xfail("Plan 02 not implemented")

def test_ctx_select_any_related_navigation():
    pytest.xfail("Plan 02 not implemented")

# Scenario schema tests (Plan 03)
def test_scenario_schema_valid_yaml_parses():
    pytest.xfail("Plan 03 not implemented")

def test_scenario_schema_missing_sender_rejected():
    pytest.xfail("Plan 03 not implemented")

def test_scenario_schema_at_ms_after_ms_mutually_exclusive():
    pytest.xfail("Plan 03 not implemented")

def test_scenario_schema_event_or_call_required():
    pytest.xfail("Plan 03 not implemented")

# Preflight multiplicity check tests (Plan 03)
def test_preflight_passes_valid_population():
    pytest.xfail("Plan 03 not implemented")

def test_preflight_rejects_missing_required_multiplicity():
    pytest.xfail("Plan 03 not implemented")

# MCP tool wrapper tests (Plan 04)
def test_simulate_domain_returns_result_dict():
    pytest.xfail("Plan 04 not implemented")

def test_simulate_domain_writes_trace_file():
    pytest.xfail("Plan 04 not implemented")

def test_simulate_class_isolated_single_class():
    pytest.xfail("Plan 04 not implemented")

def test_simulate_domain_hard_fails_on_engine_version_mismatch():
    pytest.xfail("Plan 04 not implemented")

# Trigger evaluator tests (Plan 04)
def test_trigger_fires_on_state_match():
    pytest.xfail("Plan 04 not implemented")

def test_trigger_fires_on_attr_eq_match():
    pytest.xfail("Plan 04 not implemented")

def test_trigger_disarms_after_first_fire_when_repeat_false():
    pytest.xfail("Plan 04 not implemented")

def test_trigger_rearms_when_repeat_true():
    pytest.xfail("Plan 04 not implemented")
```

Create `tests/test_simulation_e2e.py`:

```python
import pytest

def test_elevator_compiles_and_runs_multi_floor_scenario():
    pytest.xfail("Plan 04 not implemented")

def test_elevator_determinism_two_runs_identical():
    pytest.xfail("Plan 04 not implemented")

def test_elevator_key_transition_milestones_present():
    pytest.xfail("Plan 04 not implemented")
```

Create `tests/fixtures/minimal.scenario.yaml` — a single-instance single-event scenario:

```yaml
instances:
  - class: Elevator
    name: elev1
    id: {elevator_id: 1}
    state: Idle
    attrs: {current_floor: 1}
relationships: []
events:
  - event: FloorRequested
    target: elev1
    sender: elev1
    args: {floor: 2}
    at_ms: 0
triggers: []
```

Create `tests/fixtures/minimal.mocks.yaml` — empty mocks registry:

```yaml
bridges: {}
```
  </action>
  <verify>
    <automated>.venv/Scripts/python -m pytest tests/test_simulation_runner.py tests/test_simulation_e2e.py --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/test_simulation_runner.py` exists and contains the exact test function names listed above (grep confirms `def test_bundle_loader_extracts_and_verifies_version`, `def test_ctx_instance_key_populated_on_create_sync`, `def test_simulate_domain_returns_result_dict`, `def test_trigger_disarms_after_first_fire_when_repeat_false`)
    - File `tests/test_simulation_e2e.py` exists with exactly 3 test functions (`test_elevator_compiles_and_runs_multi_floor_scenario`, `test_elevator_determinism_two_runs_identical`, `test_elevator_key_transition_milestones_present`)
    - Both test files use `pytest.xfail(...)` (grep finds `pytest.xfail` in both)
    - `tests/fixtures/minimal.scenario.yaml` exists and `yaml.safe_load` succeeds on it
    - `tests/fixtures/minimal.mocks.yaml` exists and contains `bridges: {}`
    - `pytest --collect-only` exits 0 and reports at least 27 collected tests across the two new files
  </acceptance_criteria>
  <done>All stub tests collectable; collection exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Create runtime directories</name>
  <files>
    .design/bundles/.gitkeep,
    .design/scenarios/.gitkeep,
    .design/traces/.gitkeep
  </files>
  <read_first>
    .planning/phases/05.3-simulation-runner-verification/05.3-CONTEXT.md,
    tools/model_io.py
  </read_first>
  <action>
Create three empty `.gitkeep` files at:
- `.design/bundles/.gitkeep`
- `.design/scenarios/.gitkeep`
- `.design/traces/.gitkeep`

Each file is zero bytes (or may contain a single newline). Purpose: reserve canonical locations for compiled bundles (D-04 lookup target), scenario YAML storage (D-08), and trace output (D-07). `tools/simulation.py` will resolve these relative to CWD following the existing `MODEL_ROOT` pattern from `tools/model_io.py`.
  </action>
  <verify>
    <automated>.venv/Scripts/python -c "from pathlib import Path; assert Path('.design/bundles/.gitkeep').exists() and Path('.design/scenarios/.gitkeep').exists() and Path('.design/traces/.gitkeep').exists(), 'missing gitkeep'"</automated>
  </verify>
  <acceptance_criteria>
    - `.design/bundles/.gitkeep` exists (verifiable via `Path.exists()`)
    - `.design/scenarios/.gitkeep` exists
    - `.design/traces/.gitkeep` exists
    - All three parent directories are tracked by git (git status shows them as new files or clean)
  </acceptance_criteria>
  <done>Three runtime directories exist and are staged.</done>
</task>

</tasks>

<verification>
`.venv/Scripts/python -m pytest tests/test_simulation_runner.py tests/test_simulation_e2e.py --collect-only -q` exits 0.
All referenced runtime directories exist.
</verification>

<success_criteria>
Wave 0 scaffolding complete; downstream plans can reference concrete test names and fixtures.
</success_criteria>

<output>
Create `.planning/phases/05.3-simulation-runner-verification/05.3-01-SUMMARY.md`
</output>
