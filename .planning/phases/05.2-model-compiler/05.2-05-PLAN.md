---
phase: 05.2-model-compiler
plan: 05
type: execute
wave: 4
depends_on: [05.2-04]
files_modified:
  - tests/test_compiler_elevator.py
  - .planning/phases/05.2-model-compiler/05.2-VERIFICATION.md
autonomous: false
requirements: [MCP-08]
must_haves:
  truths:
    - "compile_model(examples/elevator, tmp) returns a valid .mdfbundle without raising"
    - "Generated bundle contains a generated/<Class>.py file for every concrete elevator class"
    - "Two consecutive elevator builds produce sha256-identical bundles (D-07 / SC-08)"
    - "Bundle's manifest.json contains transition tables for Elevator, CarDoor, ShaftDoor, FloorCallButton, DestFloorButton"
    - "Senescent_states on the Elevator manifest equal {Idle, Moving, Exchanging}"
    - "User has visually inspected one generated .py file and confirmed readability per D-05"
  artifacts:
    - path: "tests/test_compiler_elevator.py"
      provides: "End-to-end acceptance test against examples/elevator"
      min_lines: 80
    - path: ".planning/phases/05.2-model-compiler/05.2-VERIFICATION.md"
      provides: "Phase verification report stub for /gsd-verify-work"
  key_links:
    - from: "tests/test_compiler_elevator.py"
      to: "compiler.compile_model"
      via: "compile_model(Path('examples/elevator'), tmp_path)"
      pattern: "compile_model"
---

<objective>
Phase 5.2 acceptance: compile the elevator example end-to-end, verify SC-07 (compiles without errors) and SC-08 (deterministic bundle), inspect a generated .py file for D-05 readability, and write the verification report stub.

Purpose: Closes the phase. After this plan, Phase 5.3 (loader + simulate_domain) can begin against a working elevator bundle.
Output: Green elevator end-to-end test, human-confirmed readability of generated code, VERIFICATION.md stub.
</objective>

> **Note on `<acceptance_criteria>`:** This plan uses `<acceptance_criteria>` as the established project convention for what the executor workflow treats as `<done>`. Both names are equivalent for execution purposes — see all prior phase plans (05.1, 05.1.1) for precedent.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.2-model-compiler/05.2-CONTEXT.md
@.planning/phases/05.2-model-compiler/05.2-RESEARCH.md
@compiler/__init__.py
@examples/elevator/README.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Elevator end-to-end compile + determinism test</name>
  <files>tests/test_compiler_elevator.py</files>
  <read_first>
    - examples/elevator/ (directory layout, class list)
    - .planning/phases/05.2-model-compiler/05.2-CONTEXT.md (D-14 expected senescent set)
    - compiler/__init__.py (compile_model signature)
  </read_first>
  <behavior>
    - Test SC-07: `compile_model(Path("examples/elevator"), tmp_path)` returns a `Path` and the file exists
    - Test: bundle is a valid zip and contains `bundle.json`, `manifest.json`, `generated/__init__.py`
    - Test: bundle contains a `generated/<Class>.py` for every concrete class in the elevator class diagram
    - Test SC-08: two consecutive `compile_model` calls produce sha256-identical bundles
    - Test: parsed manifest.json has transition tables for Elevator, CarDoor, ShaftDoor, FloorCallButton, DestFloorButton
    - Test: parsed manifest.json's Elevator entry has senescent_states == {Idle, Moving, Exchanging}
    - Test: `bundle.json` parses as JSON and has keys {compiler_version, engine_version_required, pycca_version, model_hash} with no extra keys
  </behavior>
  <action>
    Replace the RED stub in tests/test_compiler_elevator.py with the full acceptance test set.

    Use `tmp_path` fixture for isolation. Compile twice into two different tmp dirs and sha256 the resulting `.mdfbundle` files. Open each bundle with `zipfile.ZipFile` to inspect contents and assert layout. Parse manifest.json with `json.loads` to assert structural invariants.

    For the senescent_states assertion, accept either a JSON list (sorted) or a JSON-encoded set representation — confirm against the actual manifest_builder serialization decision from Plan 03.

    If any assertion fails because of an unimplemented grammar / transformer construct, that is a real Phase 5.2 gap — surface it with a clear failure message naming the construct so the gap can be fixed in a follow-up plan or phase.
  </action>
  <verify>
    <automated>python -m pytest tests/test_compiler_elevator.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - SC-07 test GREEN (elevator compiles without errors)
    - SC-08 test GREEN (two builds → identical sha256)
    - All structural invariants on manifest.json GREEN
    - Senescent states match D-14 truth set
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human readability check on a generated class file</name>
  <files>.planning/phases/05.2-model-compiler/05.2-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/05.2-model-compiler/05.2-CONTEXT.md (D-05 readability requirements)
    - One freshly compiled `generated/Elevator.py` extracted from a test bundle
  </read_first>
  <action>
    Run the elevator compile to a known directory:
    ```bash
    python -c "from pathlib import Path; from compiler import compile_model; print(compile_model(Path('examples/elevator'), Path('build/elevator')))"
    ```
    Extract the bundle (it's a zip) and open `generated/Elevator.py` in your editor.

    Then create `.planning/phases/05.2-model-compiler/05.2-VERIFICATION.md` recording:
    - Bundle path
    - sha256 of two consecutive builds (must match)
    - List of generated class files
    - Notes from human review of `generated/Elevator.py`

    Then pause for the human-verify checkpoint below.
  </action>
  <what-built>
    - End-to-end elevator compile pipeline (loader → manifest → codegen → bundle)
    - Deterministic .mdfbundle output
    - Generated per-class Python source with D-05 source comments
  </what-built>
  <how-to-verify>
    1. Run: `python -c "from pathlib import Path; from compiler import compile_model; print(compile_model(Path('examples/elevator'), Path('build/elevator')))"`
    2. Extract: `python -c "import zipfile; zipfile.ZipFile('build/elevator/Elevator.mdfbundle').extractall('build/elevator/extracted')"`
    3. Open `build/elevator/extracted/generated/Elevator.py` in your editor
    4. Confirm:
       - File is black-formatted and readable
       - Every action / guard function has a `# from <model_file>:<line>` comment above it
       - Action function signatures match `def action_<name>(ctx: "SimulationContext", self_dict: dict, params: dict) -> None:`
       - Guard function signatures match `def guard_<name>(self_dict: dict, params: dict) -> bool:`
       - TRANSITION_TABLE dict is present, sorted, and contains expected (state, event) tuples
       - No `Tree(...)` literals or `None`-as-string artifacts in generated code (Pitfall 1)
    5. Open `build/elevator/extracted/bundle.json` and confirm it has `compiler_version`, `engine_version_required`, `pycca_version`, `model_hash` and no `timestamp`/`build_time` keys
    6. Run the test suite once more to confirm all phase 5.2 tests are GREEN: `python -m pytest tests/test_compiler*.py -v`
  </how-to-verify>
  <acceptance_criteria>
    - Human confirms readability of generated/Elevator.py per D-05
    - VERIFICATION.md recorded with sha256 hashes and notes
    - Full phase 5.2 test suite GREEN
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe issues found in generated code</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| compile_model output → human reviewer | Generated code is the deliverable; correctness is verified by both automated tests and human inspection. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.2-16 | Tampering | Acceptance test against elevator | mitigate | Test asserts both structural invariants AND determinism — silent regressions in either dimension fail the test. |
| T-05.2-17 | Repudiation | VERIFICATION.md record | mitigate | Hash + class list pinned in VERIFICATION.md provides an audit trail of what was actually shipped. |
| T-05.2-18 | Elevation of Privilege | Generated code human review | mitigate | Human checkpoint catches D-05 readability regressions before Phase 5.3 inherits an unreadable artifact. |
</threat_model>

<verification>
- tests/test_compiler_elevator.py GREEN
- Full suite `python -m pytest tests/ -v` GREEN (no regressions)
- VERIFICATION.md exists with bundle hash and review notes
- Human confirmed readability of one generated .py file
</verification>

<success_criteria>
- SC-07: elevator compiles to a bundle without errors
- SC-08: same input → identical bundle (sha256 equal across two consecutive builds)
- D-05: generated code human-readable with source comments
- D-14: senescent classification matches expected truth on the elevator model
- Phase 5.2 ready for /gsd-verify-work
</success_criteria>

<output>
After completion, create `.planning/phases/05.2-model-compiler/05.2-05-SUMMARY.md`
</output>
