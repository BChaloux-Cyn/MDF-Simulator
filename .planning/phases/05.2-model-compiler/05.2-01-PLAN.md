---
phase: 05.2-model-compiler
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - requirements.in
  - pycca/grammar.py
  - tests/test_compiler_grammar.py
  - tests/test_compiler_transformer.py
  - tests/test_compiler_manifest.py
  - tests/test_compiler_bundle.py
  - tests/test_compiler_elevator.py
autonomous: true
requirements: [MCP-08]
must_haves:
  truths:
    - "black is a declared project dependency and importable"
    - "pycca/grammar.py parses lambda return types as type_expr (Gap 1)"
    - "pycca/grammar.py parses capture_list with NAME or NAME.NAME items (Gap 2)"
    - "pycca/grammar.py parses select_related_stmt with traversal_chain (Gap 3)"
    - "pycca/grammar.py parses bridge_call with named_arg_list (Gap 4)"
    - "All five compiler test files exist with RED stubs covering Plan 02–05 tasks"
  artifacts:
    - path: "requirements.in"
      provides: "black>=26.0 dependency declaration"
    - path: "pycca/grammar.py"
      provides: "Patched grammar covering all 4 elevator-blocking gaps"
    - path: "tests/test_compiler_grammar.py"
      provides: "RED tests asserting parse success for the 4 gap fixtures"
      min_lines: 60
    - path: "tests/test_compiler_transformer.py"
      provides: "RED stubs for transformer construct coverage (Plan 02)"
      min_lines: 40
    - path: "tests/test_compiler_manifest.py"
      provides: "RED stubs for manifest builder + senescence + generalization cases (Plan 03)"
      min_lines: 40
    - path: "tests/test_compiler_bundle.py"
      provides: "RED stubs for bundle determinism + structure (Plan 04)"
      min_lines: 40
    - path: "tests/test_compiler_elevator.py"
      provides: "RED stub for elevator end-to-end compile (Plan 05)"
      min_lines: 20
  key_links:
    - from: "tests/test_compiler_grammar.py"
      to: "pycca/grammar.py"
      via: "STATEMENT_PARSER.parse / GUARD_PARSER.parse"
      pattern: "STATEMENT_PARSER\\.parse|GUARD_PARSER\\.parse"
---

<objective>
Wave 0: install black, fix the 4 grammar gaps blocking elevator compilation, and create RED test stubs for every downstream plan in this phase. This unblocks every later wave by guaranteeing the action grammar parses real elevator constructs and that all downstream verify commands have a target file.

Purpose: Per Nyquist rule, every later task references an automated test command. This plan creates those test files as RED stubs and reconciles known grammar gaps before any codegen work begins.
Output: requirements.in updated, pycca/grammar.py patched, 5 test files created with RED stubs.
</objective>

> **Note on `<acceptance_criteria>`:** This plan uses `<acceptance_criteria>` as the established project convention for what the executor workflow treats as `<done>`. Both names are equivalent for execution purposes — see all prior phase plans (05.1, 05.1.1) for precedent.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.2-model-compiler/05.2-CONTEXT.md
@.planning/phases/05.2-model-compiler/05.2-RESEARCH.md
@.planning/phases/05.2-model-compiler/05.2-VALIDATION.md
@pycca/grammar.py
@pycca/SYNTAX.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add black dependency and fix 4 grammar gaps</name>
  <files>requirements.in, pycca/grammar.py, tests/test_compiler_grammar.py</files>
  <read_first>
    - requirements.in (current dependency list)
    - pycca/grammar.py (rules: lambda_expr, capture_list, select_related_stmt, bridge_call)
    - .planning/phases/05.2-model-compiler/05.2-RESEARCH.md (Grammar Gap Analysis section)
    - examples/elevator/Elevator/state-diagrams/Elevator.yaml (Arriving/Exchanging entry actions for fixtures)
  </read_first>
  <behavior>
    - Test G1: Parser accepts `flat_map([] |sf: T| -> Set<T> { ... })` (lambda return = generic type)
    - Test G2: Parser accepts `var.filter([self.current_floor] |x| -> bool { ... })` (capture list with dotted name)
    - Test G3: Parser accepts `select many stops related by self->R1->R2;` (multi-hop statement form)
    - Test G4: Parser accepts `Building::IsTopFloor[floor_num: self.current_floor];` (named bridge args)
    - All four parses succeed without LarkError
    - Existing pycca tests still pass (regression check via full suite)
  </behavior>
  <action>
    Step 1: Add `black>=26.0` to requirements.in (alphabetical placement). Run `uv pip install black` to install.

    Step 2: Patch pycca/grammar.py per Research Grammar Gap Analysis (section "Grammar Gap Analysis"):
    - Gap 1: change `lambda_expr` rule to use `-> type_expr` instead of `-> NAME` (per D-08, supporting Tier 3 lambda return types).
    - Gap 2: introduce `capture_item: NAME ("." NAME)?` and change `capture_list: capture_item ("," capture_item)*`.
    - Gap 3: change `select_related_stmt` to reuse the existing `traversal_chain` rule for multi-hop traversal.
    - Gap 4: add `named_arg_list: NAME ":" expr ("," NAME ":" expr)*` and update `bridge_call` to accept either `arglist` or `named_arg_list`.

    Step 3: Create tests/test_compiler_grammar.py with one parametrized test per gap, each parsing a minimal fixture string using STATEMENT_PARSER (or GUARD_PARSER where appropriate). Tests start RED until grammar patch lands, then GREEN.

    Reference D-08 (full SYNTAX.md surface) and the Research "Grammar Gap Analysis" table for exact fix language. Do NOT introduce new grammar features beyond the 4 documented gaps in this task — that is Plan 02 territory if needed.
  </action>
  <verify>
    <automated>python -m pytest tests/test_compiler_grammar.py tests/test_pycca_grammar.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - black>=26.0 in requirements.in and `python -c "import black"` succeeds
    - All 4 grammar gap tests GREEN
    - Existing pycca grammar tests still GREEN (no regression)
    - Grammar patch confined to the 4 documented gap rules
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create RED test stubs for Plans 02–05</name>
  <files>tests/test_compiler_transformer.py, tests/test_compiler_manifest.py, tests/test_compiler_bundle.py, tests/test_compiler_elevator.py</files>
  <read_first>
    - .planning/phases/05.2-model-compiler/05.2-VALIDATION.md (Per-Task Verification Map for required test files)
    - .planning/phases/05.2-model-compiler/05.2-RESEARCH.md (Architecture Patterns + Code Examples sections)
    - .planning/phases/05.1-runtime-framework/05.1-01-PLAN.md (stub style for Wave 0 RED tests)
    - engine/manifest.py (ClassManifest / DomainManifest / TransitionEntry shape)
  </read_first>
  <action>
    Create four pytest files with RED stubs (using `pytest.skip` or `pytest.fail("not implemented")`) such that each downstream plan's `<verify>` command resolves to a real file.

    tests/test_compiler_transformer.py — one stub per pycca construct family (assignment, generate, select, bridge, lambda, if/else, for-each, method call). Reference compiler.transformer.ActionTransformer (which does not exist yet — RED).

    tests/test_compiler_manifest.py — stubs for: (a) attribute flattening (D-04), (b) generalization Case A/B/C (D-03), (c) senescent state classifier (D-14), (d) determinism — sorted dicts.

    tests/test_compiler_bundle.py — stubs for: (a) bundle layout matches D-12, (b) two consecutive builds produce identical zip bytes (D-07), (c) bundle.json contains no timestamps, (d) zip entries written in sorted order.

    tests/test_compiler_elevator.py — single stub: `compile_model(Path("examples/elevator"), tmp_path)` returns a `.mdfbundle` path and bundle is loadable.

    All stubs MUST import from `compiler.*` namespaces that do not yet exist — failing the import is the RED state.
  </action>
  <verify>
    <automated>python -m pytest tests/test_compiler_transformer.py tests/test_compiler_manifest.py tests/test_compiler_bundle.py tests/test_compiler_elevator.py --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - All 4 test files exist and pytest can collect them
    - Each file has at least the stub coverage listed in <action>
    - Stubs reference `compiler.*` modules (RED — collection or import fails as expected)
    - Files match the names listed in 05.2-VALIDATION.md Per-Task Verification Map
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Confirm RESEARCH.md Open Questions resolved</name>
  <files>.planning/phases/05.2-model-compiler/05.2-RESEARCH.md</files>
  <action>
    Verify `.planning/phases/05.2-model-compiler/05.2-RESEARCH.md` has section heading `## Open Questions (RESOLVED)` and that all 4 questions carry an explicit Resolution paragraph (D-08 Tier 3 stubs, guard_fn 2-arg, D-13 encoding flip, callable stripping). This was done during plan revision; this task is a no-op safety check. If the heading is still `## Open Questions` (no RESOLVED suffix), update it and write the 4 resolutions per the revision notes. Do NOT modify any other research content.
  </action>
  <verify>
    <automated>python -c "import pathlib,sys; t=pathlib.Path('.planning/phases/05.2-model-compiler/05.2-RESEARCH.md').read_text(encoding='utf-8'); sys.exit(0 if '## Open Questions (RESOLVED)' in t else 1)"</automated>
  </verify>
  <acceptance_criteria>
    - RESEARCH.md heading is `## Open Questions (RESOLVED)`
    - All 4 questions have explicit Resolution paragraphs
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| modeler YAML → compiler | Untrusted-by-mistake input: malformed YAML or pycca source from a developer's local model directory. No network surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.2-01 | Tampering | pycca/grammar.py patches | mitigate | Regression test suite (existing pycca tests) must remain GREEN — locks against accidental scope creep. |
| T-05.2-02 | Denial of Service | Lark parser on pathological action body | accept | Local-only tooling, single-developer surface, no remote input path; bounded by file size of model YAML. |
| T-05.2-03 | Information Disclosure | requirements.in dependency add | accept | black is a well-known PSF-adjacent tool; pinned floor version; supply chain risk equivalent to existing deps. |
</threat_model>

<verification>
- Grammar fix tests GREEN
- All 4 downstream test files exist and collect cleanly (RED on imports — expected)
- `python -c "import black; print(black.__version__)"` reports >=26.0
- No regressions in existing test suite: `python -m pytest tests/ -x -q` reports the only failures are the new RED stubs
</verification>

<success_criteria>
- 4 grammar gaps closed; elevator-blocking parse failures eliminated
- black installed and declared
- 5 test files exist (1 GREEN, 4 RED stubs)
- Wave 0 unblocks Plan 02 (transformer can build against working grammar)
</success_criteria>

<output>
After completion, create `.planning/phases/05.2-model-compiler/05.2-01-SUMMARY.md`
</output>
