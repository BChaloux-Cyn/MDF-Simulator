---
phase: 05.2-model-compiler
plan: 03
type: execute
wave: 2
depends_on: [05.2-02]
files_modified:
  - compiler/loader.py
  - compiler/manifest_builder.py
  - compiler/senescence.py
  - tests/test_compiler_manifest.py
autonomous: true
requirements: [MCP-08]
must_haves:
  truths:
    - "compiler.loader.load_model returns ClassDiagramFile/StateDiagramFile/TypesFile collections from a model root"
    - "manifest_builder.build_domain_manifest produces a deterministic DomainManifest with sorted dicts"
    - "Generalization Case A/B/C are encoded per D-03 (no merging in Case C)"
    - "Subtype attributes flatten supertype attributes per D-04 with subtype-wins on conflict"
    - "Senescent state classifier matches D-14 (Idle/Moving/Exchanging senescent; Departing/Floor_Updating/Arriving transient on the elevator model)"
    - "can't_happen = absent cell, event_ignored = present-all-None cell (matches scheduler ground truth)"
  artifacts:
    - path: "compiler/loader.py"
      provides: "Model root → schema dataclasses"
      exports: ["load_model", "LoadedModel"]
      min_lines: 80
    - path: "compiler/manifest_builder.py"
      provides: "Schema → DomainManifest with all D-03/D-04/D-07 rules"
      exports: ["build_domain_manifest", "build_class_manifest"]
      min_lines: 200
    - path: "compiler/senescence.py"
      provides: "Static senescent state classifier per D-14"
      exports: ["classify_senescent_states", "has_self_generate"]
      min_lines: 60
  key_links:
    - from: "compiler/manifest_builder.py"
      to: "compiler/senescence.py"
      via: "classify_senescent_states call per ClassManifest"
      pattern: "classify_senescent_states"
    - from: "compiler/manifest_builder.py"
      to: "engine/manifest.py"
      via: "TYPE_CHECKING import of TypedDict shapes"
      pattern: "TYPE_CHECKING"
---

<objective>
Build the loader (YAML → schema objects) and manifest builder (schema → DomainManifest) including: D-03 generalization handling, D-04 attribute flattening, D-06 type system mapping, D-07 determinism (sorted dicts), D-13 transition table encoding (matching scheduler ground truth), and D-14 senescent state classification.

Purpose: Produce the fully-populated DomainManifest that Plan 04 will serialize and Plan 05 will validate against the elevator model.
Output: compiler/loader.py, compiler/manifest_builder.py, compiler/senescence.py.
</objective>

> **Note on `<acceptance_criteria>`:** This plan uses `<acceptance_criteria>` as the established project convention for what the executor workflow treats as `<done>`. Both names are equivalent for execution purposes — see all prior phase plans (05.1, 05.1.1) for precedent.

> **D-13 Override (transition table encoding):** Scheduler ground truth takes precedence over CONTEXT.md D-13 text. Correct encoding: **`can't_happen = absent cell`** (omit from `transition_table` dict), **`event_ignored = present cell with all-None fields`** (`next_state=None, action_fn=None, guard_fn=None`). CONTEXT.md D-13 currently describes the inverse and is **superseded** — Task 2 below amends it. This is recorded in 05.2-RESEARCH.md "Open Questions (RESOLVED)" §3 and verified by cross-test against `engine/scheduler.py` tests.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.2-model-compiler/05.2-CONTEXT.md
@.planning/phases/05.2-model-compiler/05.2-RESEARCH.md
@schema/yaml_schema.py
@engine/manifest.py
@engine/scheduler.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Loader + senescent classifier</name>
  <files>compiler/loader.py, compiler/senescence.py, tests/test_compiler_manifest.py</files>
  <read_first>
    - schema/yaml_schema.py (ClassDiagramFile, StateDiagramFile, TypesFile shapes)
    - examples/elevator/ (file layout and an Elevator state machine)
    - .planning/phases/05.2-model-compiler/05.2-CONTEXT.md (D-14)
    - .planning/phases/05.2-model-compiler/05.2-RESEARCH.md ("Senescent State Classifier" code example)
  </read_first>
  <behavior>
    - Test: `load_model(Path("examples/elevator"))` returns a LoadedModel with class_diagrams, state_diagrams, types_file populated
    - Test: missing required directory raises CompileError (not silent empty result)
    - Test: `has_self_generate(parser.parse("generate Foo to self;"))` is True
    - Test: `has_self_generate(parser.parse("generate Foo to other;"))` is False
    - Test: `has_self_generate` recurses into if/for blocks (conservative — guarded self-generate still counts)
    - Test: classifier on the elevator Elevator state machine returns {Idle, Moving, Exchanging} as senescent and excludes {Departing, Floor_Updating, Arriving}
  </behavior>
  <action>
    Implement `compiler/loader.py`:
    - `@dataclass class LoadedModel: class_diagrams: dict[str, ClassDiagramFile]; state_diagrams: dict[str, StateDiagramFile]; types_file: TypesFile | None; root: Path`
    - `load_model(model_root: Path) -> LoadedModel` walks the standard layout used by the elevator example. Use schema.yaml_schema Pydantic loaders. Accumulate any file-not-found / parse errors via ErrorAccumulator and raise per D-09.

    Implement `compiler/senescence.py` per Research code example:
    - `has_self_generate(tree) -> bool` walks the Lark tree looking for `generate_stmt` nodes whose target NAME (last NAME child) is `self`. Conservative — recurses into all children including conditional blocks (D-14 rationale).
    - `classify_senescent_states(state_machine, parser) -> set[str]` returns the set of state names whose entry action contains no self-generate. States with no entry action are senescent by definition.

    Drive each behavior bullet with a unit test before implementing.
  </action>
  <verify>
    <automated>python -m pytest tests/test_compiler_manifest.py -k "loader or senescen" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Loader returns populated LoadedModel for elevator example
    - has_self_generate correctly detects target=self vs target=other
    - Elevator senescent classification matches D-14 expected truth set
    - All loader/senescence tests GREEN
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Manifest builder with D-03/D-04/D-07/D-13 encoding</name>
  <files>compiler/manifest_builder.py, tests/test_compiler_manifest.py</files>
  <read_first>
    - engine/manifest.py (ClassManifest, DomainManifest, TransitionEntry shapes)
    - engine/scheduler.py lines 380–420 (can't_happen / event_ignored ground truth encoding)
    - .planning/phases/05.2-model-compiler/05.2-RESEARCH.md ("Generalization Case Dispatcher", "Transition Table Builder", "Critical Finding: can't_happen Encoding Mismatch")
    - .planning/phases/05.2-model-compiler/05.2-CONTEXT.md (D-03, D-04, D-06, D-07, D-13, D-14)
    - schema/yaml_schema.py (ClassDef.partitions, generalization shape)
  </read_first>
  <behavior>
    - Test (D-04): subtype manifest contains all supertype attributes plus its own; on name conflict, subtype wins
    - Test (D-03 Case A): supertype is entity (no SM), subtype has SM → subtype.transition_table built from own SM
    - Test (D-03 Case B): supertype has SM, subtype has no SM redefinition → subtype.transition_table is a copy of supertype's
    - Test (D-03 Case C): supertype has SM, subtype has its own SM → subtype.transition_table built ONLY from subtype's SM (no merge)
    - Test (D-13): event_ignored cell present with next_state=action_fn=guard_fn=None
    - Test (D-13): can't_happen cell absent from transition_table dict
    - Test (D-13 cross-check): scheduler's existing test_cant_happen_error_microstep encoding still asserts absent=cant_happen (regression guard)
    - Test (D-07): two consecutive `build_domain_manifest` calls on the same input produce dicts with identical key ordering after `json.dumps(..., sort_keys=True)`
    - Test (D-14): senescent_states populated on each ClassManifest via classify_senescent_states
    - Test (D-06): MDF enum types mapped to enum.Enum subclass placeholder; typedefs mapped to NewType placeholder; refinement constraints noted in manifest for runtime validators
  </behavior>
  <action>
    Implement `compiler/manifest_builder.py`:
    - `build_class_manifest(class_def, state_diagrams, supertype, supertype_manifest, parser, types) -> ClassManifest` per Research "Generalization Case Dispatcher" example. Handle Cases A/B/C exactly as D-03 specifies.
    - Attribute flattening per D-04. Subtype wins on name conflict. Sort attribute dict by key (D-07).
    - `_build_transition_table(sd) -> dict[tuple[str, str], TransitionEntry]` per Research example. Encoding: explicit transition → entry present; can't_happen → absent (omit cell); event_ignored → present with all-None. Action_fn and guard_fn left as None placeholders here — Plan 04 codegen fills function names. Cross-check against scheduler tests via a test that imports and runs scheduler's test_cant_happen path against a hand-built manifest.
    - `build_domain_manifest(loaded: LoadedModel, parser: Lark) -> DomainManifest` traverses all classes, builds inheritance tree (parents-first), invokes build_class_manifest, calls classify_senescent_states for each class, populates `subtypes` field by post-walk. Sort every dict by key per D-07.
    - Type system: build a type registry mapping enum names → list of values, typedef names → underlying type, refinement constraints → list of (attr, validator-spec) tuples. Stash on DomainManifest in a deterministic shape Plan 04 can serialize.

    Use `from typing import TYPE_CHECKING` + `if TYPE_CHECKING: from engine.manifest import ...` to import types without runtime engine dependency (D-11).

    **Amend CONTEXT.md D-13:** Edit `.planning/phases/05.2-model-compiler/05.2-CONTEXT.md` so D-13 records the correct (scheduler-derived) encoding. The two bullets must read:
    - `**event_ignored** -> cell present in transition_table with next_state=None, action_fn=None, guard_fn=None (scheduler treats all-None entry as silent ignore)`
    - `**can't_happen** -> cell absent from transition_table (scheduler emits ErrorMicroStep when key is missing)`
    Append a one-line note: `> Encoding flipped from initial draft on 2026-04-06 to match engine/scheduler.py ground truth — scheduler tests are authoritative.`
  </action>
  <verify>
    <automated>python -m pytest tests/test_compiler_manifest.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - All behavior bullets have GREEN tests
    - No `import engine` outside `TYPE_CHECKING` block in manifest_builder.py
    - Determinism test passes (two builds → identical sorted JSON)
    - Generalization Cases A/B/C tests verify D-03 encoding exactly
    - Transition table encoding matches scheduler ground truth (verified by cross-test)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Schema dataclass → DomainManifest | Already-validated input from schema/yaml_schema.py Pydantic models. Trust boundary mostly closed at schema layer. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.2-08 | Tampering | Generalization merging logic | mitigate | D-03 explicit case-by-case test coverage prevents accidental supertype/subtype shadowing — each case has a dedicated unit test. |
| T-05.2-09 | Tampering | Determinism of manifest dict ordering | mitigate | Two-build identity test (D-07) on every plan-04 invocation; sorted() applied at every dict construction site. |
| T-05.2-10 | Repudiation | Senescent classification | mitigate | Conservative syntactic rule means classification is reproducible from source AST without flow-analysis interpretation — auditable from the YAML alone. |
| T-05.2-11 | Spoofing | TYPE_CHECKING import boundary | mitigate | D-11 hard rule enforced by a unit test that grep's compiler/*.py for runtime `import engine` statements. |
</threat_model>

<verification>
- All manifest tests GREEN
- Loader handles missing/malformed model directories with CompileError
- Senescent classifier verified against the elevator model expected truth set (D-14)
- D-13 encoding matches scheduler.py ground truth (verified by cross-test)
- D-11 boundary preserved (compiler does not import engine at runtime)
</verification>

<success_criteria>
- DomainManifest fully populated for any input model (subject to grammar coverage)
- Determinism guaranteed at the manifest layer (Plan 04 inherits this property)
- Plan 04 can call `build_domain_manifest` and proceed straight to codegen + packaging
</success_criteria>

<output>
After completion, create `.planning/phases/05.2-model-compiler/05.2-03-SUMMARY.md`
</output>
