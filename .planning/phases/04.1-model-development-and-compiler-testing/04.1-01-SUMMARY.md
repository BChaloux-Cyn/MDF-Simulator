---
phase: 04.1-model-development-and-compiler-testing
plan: "01"
subsystem: validation
tags: [pydantic, validation, schema, testing]

# Dependency graph
requires:
  - phase: 03-validation-tool
    provides: "validate_model, validate_class, validate_domain implementations"
provides:
  - "validation.py free of AttributeError on ClassDef.formalizes access"
  - "test_yaml_schema.py free of associative/formalizes references"
affects:
  - 04.1-02-PLAN.md
  - all subsequent 04.1 plans that invoke validate_model or validate_class

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Dead code removed — schema concepts removed from schema must be removed from validation and tests in lock-step"

key-files:
  created: []
  modified:
    - tools/validation.py
    - tests/test_yaml_schema.py

key-decisions:
  - "Comment references to removed schema concepts (formalizes) were also removed — not just live code blocks"
  - "test_valid_class_diagram_accepted asserts updated to match single-class fixture (len==1) after ValveCommand removal"
  - "Two pre-existing visibility/scope test failures deferred — pre-existed this plan, out of scope"

patterns-established:
  - "When schema field removed: audit both validation.py and test_yaml_schema.py for all references (code + comments)"

requirements-completed:
  - SCHEMA-REPAIR-01

# Metrics
duration: 10min
completed: 2026-03-17
---

# Phase 04.1 Plan 01: Remove Dead Formalizes Blocks Summary

**Deleted two `if cls.formalizes` blocks from validation.py and the `stereotype: associative` fixture+test from test_yaml_schema.py, eliminating the AttributeError that crashed validate_model and validate_class.**

## Performance

- **Duration:** ~10 min
- **Started:** 2026-03-17
- **Completed:** 2026-03-17
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments

- `validate_model` and `validate_class` no longer raise `AttributeError` on `ClassDef.formalizes`
- `test_yaml_schema.py` fixture and test aligned with current schema (no `associative` stereotype, no `formalizes` field)
- 35 tests pass across `test_yaml_schema.py` and `test_validation.py`; 2 pre-existing visibility failures remain (logged, deferred)

## Task Commits

1. **Task 1: Remove formalizes blocks from validation.py** - `c59f9ae` (fix)
2. **Task 2: Remove associative fixture from test_yaml_schema.py** - `8b4ceea` (fix)

## Files Created/Modified

- `tools/validation.py` — Removed `if cls.formalizes` block from `_check_referential_integrity_class_diagram` and `if cls_def.formalizes` block from `validate_class`; removed two stale comments referencing the formalizes concept
- `tests/test_yaml_schema.py` — Removed `ValveCommand` associative class from `VALID_CLASS_DIAGRAM`, removed `test_associative_missing_formalizes_rejected`, updated `test_valid_class_diagram_accepted` assertions

## Decisions Made

- Comment references to `formalizes` in `validation.py` were also removed (not just live code) to satisfy the zero-match acceptance criteria and keep the codebase clean.
- `test_valid_class_diagram_accepted` asserts `len(result.classes) == 1` and drops the `assoc_class` block after the `ValveCommand` fixture entry was removed. The rest of the test is unchanged.

## Deviations from Plan

None — plan executed exactly as written.

## Issues Encountered

Two pre-existing test failures were discovered in `test_yaml_schema.py`:

- `test_attribute_visibility_scope_defaults` — expects `attr.visibility == "private"` but schema defaults to `"public"`
- `test_attribute_visibility_scope_explicit` — uses `"protected"` visibility which schema rejects (only `"public"` or `"private"` allowed)

Both failures pre-date this plan. They are logged in `deferred-items.md` for a follow-on fix. These tests are not related to `formalizes` or the `associative` stereotype.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- `validate_model` and `validate_class` are now safe to invoke without triggering `AttributeError`
- Phase 04.1 Plan 02 can proceed to build the elevator domain model

---
*Phase: 04.1-model-development-and-compiler-testing*
*Completed: 2026-03-17*
