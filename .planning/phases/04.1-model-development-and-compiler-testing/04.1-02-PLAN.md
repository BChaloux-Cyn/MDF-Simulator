---
phase: 04.1-model-development-and-compiler-testing
plan: "02"
type: execute
wave: 2
depends_on:
  - "04.1-01"
files_modified:
  - examples/elevator/.design/model/Elevator/class-diagram.yaml
  - schema/yaml_schema.py
  - tools/validation.py
autonomous: true
requirements:
  - SCHEMA-REPAIR-02
  - ELV-001
  - ELV-003
must_haves:
  truths:
    - "FloorIndicator is declared as stereotype: active with R10 (FloorIndicator-Elevator) and R13 (FloorIndicator-Floor)"
    - "The old M:M R10 (Elevator-Floor) association is gone from class-diagram.yaml"
    - "ElevatorCall and FloorCall no longer re-declare call_id"
    - "Elevator.r14_request_id referential points to new R14 head-pointer association"
    - "pytest tests/test_validation.py passes after all changes"
  artifacts:
    - path: "examples/elevator/.design/model/Elevator/class-diagram.yaml"
      provides: "FloorIndicator active; R10 FloorIndicator-Elevator; R13 Floor-FloorIndicator; R14 Elevator-Request head pointer"
      contains: "stereotype: active"
    - path: "schema/yaml_schema.py"
      provides: "Helper _get_effective_attributes for subtype inheritance"
      contains: "_get_effective_attributes"
    - path: "tools/validation.py"
      provides: "Validator flags subtype re-declaring supertype identifier"
  key_links:
    - from: "examples/elevator/.design/model/Elevator/class-diagram.yaml"
      to: "schema/yaml_schema.py ClassDef"
      via: "stereotype: active (not associative)"
    - from: "schema/yaml_schema.py _get_effective_attributes"
      to: "tools/validation.py _check_class_referential_integrity"
      via: "called to compute effective attribute set before subtype redeclaration check"
---

<objective>
Three structural model and schema changes required before action bodies can be authored:
1. Migrate FloorIndicator from associative to active with two explicit associations.
2. Fix ELV-001: validator rejects subtypes that re-declare supertype identifier attributes;
   remove redundant call_id from ElevatorCall and FloorCall.
3. Fix ELV-003: replace the wrong r_queue_head_id referential: R2 on Elevator with
   r14_request_id referential: R14, and add R14 head-pointer association.

Purpose: Action bodies authored in Plans 05/06 traverse these associations by name.
Structural correctness must precede action body authoring.

Output: Updated class-diagram.yaml, updated yaml_schema.py validator helper, updated
validation.py with subtype redeclaration check.
</objective>

<execution_context>
@C:/Users/bchaloux/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/bchaloux/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-model-development-and-compiler-testing/04.1-CONTEXT.md
@.planning/phases/04.1-model-development-and-compiler-testing/04.1-RESEARCH.md
@pycca/SYNTAX.md
@schema/COMPILATION.md
@issues/elevator-001-subtype-relvar-inheritance.md
@issues/elevator-003-linked-list-r9-metamodel.md
@.planning/phases/04.1-model-development-and-compiler-testing/04.1-01-SUMMARY.md

<interfaces>
From schema/yaml_schema.py ClassDef (confirmed):
  name: str
  stereotype: Literal["entity", "active"]
  partitions: list[Partition] = []
  specializes: str | None = None
  attributes: list[Attribute] = []

From schema/yaml_schema.py Attribute:
  name: str
  type: str
  identifier: bool = False
  referential: str | None = None

From tools/validation.py _make_issue:
  def _make_issue(issue, location, value=None, fix=None, severity="error") -> dict

COMPILATION.md referential naming convention:
  r{N}_{target_identifier_attr}
  e.g. R10 -> Elevator (identifier: elevator_id) -> r10_elevator_id
  e.g. R13 -> Floor (identifier: floor_num) -> r13_floor_num
  e.g. R14 -> Request (identifier: request_id) -> r14_request_id
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate FloorIndicator and fix ELV-001/003 in class-diagram.yaml</name>
  <files>examples/elevator/.design/model/Elevator/class-diagram.yaml</files>
  <read_first>
    - examples/elevator/.design/model/Elevator/class-diagram.yaml (read entire file first)
    - schema/COMPILATION.md (referential naming rules)
    - issues/elevator-003-linked-list-r9-metamodel.md
  </read_first>
  <action>
Make the following six changes to class-diagram.yaml. Read the full file first to confirm
current line positions, then apply each change precisely.

CHANGE 1 — FloorIndicator stereotype (replace entire class entry):
Old:
  name: FloorIndicator
  stereotype: associative
  formalizes: R10
  attributes with r10_elevator_id and r10_floor_num both referential: R10

New (exact replacement):
  - name: FloorIndicator
    stereotype: active
    attributes:
      - name: indicator_id
        type: UniqueID
        identifier: true
      - name: direction
        type: Direction
      - name: r10_elevator_id
        type: UniqueID
        referential: R10
      - name: r13_floor_num
        type: FloorNumber
        referential: R13

Note: r10_floor_num becomes r13_floor_num because Floor is now R13.

CHANGE 2 — Elevator.r_queue_head_id -> r14_request_id:
In the Elevator class attributes block, replace:
      - name: r_queue_head_id
        type: UniqueID
        referential: R2
With:
      - name: r14_request_id
        type: UniqueID
        referential: R14

CHANGE 3 — R10 association: replace M:M Elevator-Floor with 1:0..* Elevator-FloorIndicator:
Old R10 entry (point_1: Elevator, point_2: Floor, both 0..*):
Replace with:
  - name: R10
    point_1: Elevator
    point_2: FloorIndicator
    1_mult_2: "0..*"
    2_mult_1: "1"
    1_phrase_2: has indicator at
    2_phrase_1: shows floor for

CHANGE 4 — Add R13 association after R12:
  - name: R13
    point_1: Floor
    point_2: FloorIndicator
    1_mult_2: "0..*"
    2_mult_1: "1"
    1_phrase_2: has indicator
    2_phrase_1: is located on

CHANGE 5 — Add R14 association after R13:
  - name: R14
    point_1: Elevator
    point_2: Request
    1_mult_2: "0..1"
    2_mult_1: "0..1"
    1_phrase_2: has as queue head
    2_phrase_1: is queue head for

CHANGE 6 — ELV-001: Remove redundant call_id from ElevatorCall and FloorCall.

ElevatorCall after fix (only destination_floor remains):
  - name: ElevatorCall
    stereotype: active
    specializes: R5
    attributes:
      - name: destination_floor
        type: FloorNumber

FloorCall after fix (call_id removed; floor_num and direction remain):
  - name: FloorCall
    stereotype: active
    specializes: R5
    attributes:
      - name: floor_num
        type: FloorNumber
      - name: direction
        type: CallDirection
  </action>
  <verify>
    <automated>cd /c/Users/bchaloux/Local_Documents/Local_Repos/mdf-simulator && python -c "
import yaml
from pathlib import Path
data = yaml.safe_load(Path('examples/elevator/.design/model/Elevator/class-diagram.yaml').read_text())
classes = {c['name']: c for c in data['classes']}
assocs = {a['name']: a for a in data['associations']}
assert classes['FloorIndicator']['stereotype'] == 'active'
assert 'formalizes' not in classes['FloorIndicator']
fi_attrs = [a['name'] for a in classes['FloorIndicator']['attributes']]
assert 'r13_floor_num' in fi_attrs
assert 'r10_floor_num' not in fi_attrs
assert 'R13' in assocs and assocs['R13']['point_2'] == 'FloorIndicator'
assert 'R14' in assocs and assocs['R14']['point_1'] == 'Elevator'
assert assocs['R10']['point_2'] == 'FloorIndicator'
elv_attrs = [a['name'] for a in classes['Elevator']['attributes']]
assert 'r14_request_id' in elv_attrs
assert 'r_queue_head_id' not in elv_attrs
assert 'call_id' not in [a['name'] for a in classes['ElevatorCall']['attributes']]
assert 'call_id' not in [a['name'] for a in classes['FloorCall']['attributes']]
print('All class-diagram assertions passed')
"
  <acceptance_criteria>
    - grep for "stereotype: active" in class-diagram.yaml returns FloorIndicator line
    - grep for "formalizes" in class-diagram.yaml returns zero matches
    - grep for "r13_floor_num" in class-diagram.yaml returns a match
    - grep for "r10_floor_num" in class-diagram.yaml returns zero matches
    - grep for "R14" in class-diagram.yaml returns two matches (class attribute + association)
    - grep for "r_queue_head_id" in class-diagram.yaml returns zero matches
    - python validation script above exits 0 with "All class-diagram assertions passed"
  </acceptance_criteria>
  <done>class-diagram.yaml has FloorIndicator as active class, R10/R13/R14 correct, ELV-001/003 fixed</done>
</task>

<task type="auto">
  <name>Task 2: Add subtype redeclaration check to yaml_schema.py and validation.py</name>
  <files>schema/yaml_schema.py, tools/validation.py</files>
  <read_first>
    - schema/yaml_schema.py (read entire file)
    - tools/validation.py (read entire file, focus on _check_class_referential_integrity)
    - issues/elevator-001-subtype-relvar-inheritance.md
  </read_first>
  <action>
Add a helper function and a validation check for ELV-001.

STEP 1 — Add _get_effective_attributes helper to tools/validation.py.

Add this function near the top of validation.py, after the existing helper functions
(_make_issue, _is_valid_type, etc.) and before _check_class_referential_integrity:

    def _get_effective_attributes(cls, class_map: dict) -> list:
        """Return cls.attributes merged with supertype identifier attributes for subtypes.

        If cls.specializes is not None, find the supertype that declares a partition
        for cls.specializes and prepend its identifier attributes to cls.attributes.
        This implements ELV-001: subtypes inherit supertype identifier attributes.
        """
        if cls.specializes is None:
            return list(cls.attributes)
        # Find the supertype: a class with a partition whose name matches cls.specializes
        for candidate in class_map.values():
            for partition in candidate.partitions:
                if partition.name == cls.specializes:
                    # Return supertype identifiers + subtype attributes
                    supertype_ids = [a for a in candidate.attributes if a.identifier]
                    return supertype_ids + list(cls.attributes)
        return list(cls.attributes)

STEP 2 — Add subtype redeclaration check inside _check_class_referential_integrity.

In _check_class_referential_integrity, after the existing loop over classes (after the
specializes check block and before the attribute type check loop), add:

        # ELV-001: check that subtypes do not re-declare identifier attributes
        # that already exist on the supertype.
        if cls.specializes is not None:
            effective_attrs = _get_effective_attributes(cls, class_map)
            subtype_attr_names = {a.name for a in cls.attributes}
            for supertype_attr in effective_attrs:
                if supertype_attr.identifier and supertype_attr.name in subtype_attr_names:
                    issues.append(_make_issue(
                        issue=(
                            f"Subtype '{cls.name}' re-declares identifier attribute "
                            f"'{supertype_attr.name}' that exists on its supertype"
                        ),
                        location=f"{loc_cd}::classes.{cls.name}.attributes.{supertype_attr.name}",
                        value=supertype_attr.name,
                        fix=(
                            f"Remove '{supertype_attr.name}' from '{cls.name}' — "
                            f"it is inherited from the supertype via '{cls.specializes}'"
                        ),
                    ))

Note: class_map must be available in that scope. Confirm it is already built as a dict
keyed by class name in _check_class_referential_integrity. If not, build it at the top
of the function: class_map = {c.name: c for c in cd.classes}
  </action>
  <verify>
    <automated>pytest tests/test_validation.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "_get_effective_attributes" schema/yaml_schema.py returns zero (helper lives in validation.py not schema)
    - grep -n "_get_effective_attributes" tools/validation.py returns at least 2 matches (definition + call site)
    - pytest tests/test_validation.py -x -q exits 0 with all tests passing
    - python -c "from tools.validation import validate_model" exits 0
  </acceptance_criteria>
  <done>_get_effective_attributes defined in validation.py; subtype redeclaration check added; test suite green</done>
</task>

</tasks>

<verification>
Run full test suite to confirm no regressions:
pytest tests/test_yaml_schema.py tests/test_validation.py -x -q

Additionally confirm the elevator model YAML loads cleanly through the schema:
cd examples/elevator && python -c "
import yaml
from schema.yaml_schema import ClassDiagramFile
data = yaml.safe_load(open('.design/model/Elevator/class-diagram.yaml').read())
cd_file = ClassDiagramFile(**data)
print('Schema validation passed:', len(cd_file.classes), 'classes')
"
</verification>

<success_criteria>
- class-diagram.yaml: FloorIndicator stereotype: active, R10 points to FloorIndicator, R13 added, R14 added
- class-diagram.yaml: ElevatorCall and FloorCall have no call_id attribute
- class-diagram.yaml: Elevator has r14_request_id (not r_queue_head_id)
- tools/validation.py: contains _get_effective_attributes function definition
- pytest tests/test_yaml_schema.py tests/test_validation.py -x -q exits 0
- issues/elevator-001-subtype-relvar-inheritance.md: update Fix Applied section with what was done
- issues/elevator-003-linked-list-r9-metamodel.md: update Fix Applied section with Option A description
</success_criteria>

<output>
After completion, create .planning/phases/04.1-model-development-and-compiler-testing/04.1-02-SUMMARY.md
</output>
