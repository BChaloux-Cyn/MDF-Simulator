---
phase: 04.1-model-development-and-compiler-testing
plan: "06"
type: execute
wave: 6
depends_on:
  - "04.1-05"
files_modified:
  - examples/elevator/.design/model/Elevator/state-diagrams/Elevator.yaml
  - examples/elevator/.design/model/Elevator/state-diagrams/Dispatcher.yaml
  - examples/elevator/.design/model/Elevator/state-diagrams/ElevatorCall.yaml
  - examples/elevator/.design/model/Elevator/state-diagrams/FloorCall.yaml
  - examples/elevator/.design/model/Elevator/state-diagrams/Request.yaml
  - examples/elevator/.design/model/Elevator/state-diagrams/CallButton.yaml
  - tests/test_elevator.py
autonomous: true
requirements:
  - ACTION-BODIES-02
  - TEST-ELEVATOR
must_haves:
  truths:
    - "validate_model('Elevator') returns an empty list"
    - "pytest tests/test_elevator.py passes with zero failures"
    - "pytest tests/ -x -q passes with full suite green"
    - "Elevator.yaml entry actions use brace-style if and correct pycca syntax throughout"
    - "No action body contains bare class name as generate target"
    - "No action body uses old create object of syntax"
    - "No action body uses old if expr; end if; syntax (brace style only)"
  artifacts:
    - path: "tests/test_elevator.py"
      provides: "End-to-end validate_model zero-issue check"
      contains: "def test_elevator_model_clean"
    - path: "examples/elevator/.design/model/Elevator/state-diagrams/Elevator.yaml"
      provides: "Complete action bodies; all pycca syntax correct"
    - path: "examples/elevator/.design/model/Elevator/state-diagrams/Dispatcher.yaml"
      provides: "Complete transition action bodies"
  key_links:
    - from: "tests/test_elevator.py test_elevator_model_clean"
      to: "tools/validation.py validate_model"
      via: "monkeypatch.chdir('examples/elevator'); issues = validate_model('Elevator'); assert issues == []"
    - from: "Elevator.yaml action bodies"
      to: "pycca/grammar.py STATEMENT_PARSER"
      via: "validation.py pycca syntax pre-check called during validate_model"
---

<objective>
Author complete simulate-ready pycca action bodies for the six more complex active
classes: Elevator, Dispatcher, ElevatorCall, FloorCall, Request, CallButton.
Then write test_elevator.py to prove validate_model returns zero issues on the complete
elevator domain.

Purpose: This is the phase gate — validate_model zero issues proves the full pipeline
(schema, model structure, action body syntax) is correct. test_elevator.py becomes a
permanent regression guard for the elevator reference model.

Output: Six corrected/completed state diagram YAML files; tests/test_elevator.py passing.
</objective>

<execution_context>
@C:/Users/bchaloux/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/bchaloux/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-model-development-and-compiler-testing/04.1-CONTEXT.md
@.planning/phases/04.1-model-development-and-compiler-testing/04.1-RESEARCH.md
@pycca/SYNTAX.md
@schema/COMPILATION.md
@examples/elevator/.design/model/Elevator/class-diagram.yaml
@.planning/phases/04.1-model-development-and-compiler-testing/04.1-05-SUMMARY.md
@.planning/phases/04.1-model-development-and-compiler-testing/04.1-04-SUMMARY.md

<interfaces>
Extended pycca grammar (Plan 04, confirmed by test_pycca.py green):
  select any var related by self->RN;
  generate Event(param: val) to var;
  if (expr) { stmts } else { stmts }
  create var of Class;
  relate var to self across RN;
  delete var;
  self.attr = rcvd_evt.param;
  self.attr = var.attr;

Class associations relevant to action bodies (from class-diagram.yaml after Plans 02-03):
  R1:  Elevator 1 -- 1 Door (r1_elevator_id on Door)
  R2:  Elevator 1 -- 0..* Request (r2_elevator_id on Request)
  R5:  Call 1 -- 1 ElevatorCall; Call 1 -- 1 FloorCall (r5_call_id on subtypes... inherits via ELV-001 fix)
  R7:  CallButton 1 -- 0..1 Call (r7_button_id on Call)
  R8:  Call 0..1 -- 0..1 Request (r8_call_id on Call, r8_request_id on Request)
  R9:  Request 0..1 -- 0..1 Request (r9_next_id on Request)
  R11: Elevator 1 -- 1 Shaft (no referential on either — traversal only via related by)
  R12: Elevator 1 -- 1 ElevatorIndicator (no referential — traversal only)
  R14: Elevator 0..1 -- 0..1 Request (r14_request_id on Elevator) — head pointer

Subtype inheritance (ELV-001 fix, Plan 02):
  ElevatorCall inherits call_id, r7_button_id, r8_request_id from supertype Call
  FloorCall inherits call_id, r7_button_id, r8_request_id from supertype Call
  Action bodies can reference these inherited attributes directly (e.g. self.r7_button_id)

NOTE on Call supertype referentials:
  r7_button_id: referential on Call for R7 (CallButton is the target; button_id is its identifier)
  r8_request_id: referential on Call for R8 (Request is the target; request_id is its identifier)
  These are declared on Call and inherited by ElevatorCall and FloorCall.

validate_model uses monkeypatch.chdir pattern:
  MODEL_ROOT = Path(".design/model") anchored to CWD
  Tests must call monkeypatch.chdir("examples/elevator") before validate_model("Elevator")
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Complete action bodies for Elevator.yaml, ElevatorCall.yaml, FloorCall.yaml, Request.yaml, CallButton.yaml</name>
  <files>
    examples/elevator/.design/model/Elevator/state-diagrams/Elevator.yaml,
    examples/elevator/.design/model/Elevator/state-diagrams/ElevatorCall.yaml,
    examples/elevator/.design/model/Elevator/state-diagrams/FloorCall.yaml,
    examples/elevator/.design/model/Elevator/state-diagrams/Request.yaml,
    examples/elevator/.design/model/Elevator/state-diagrams/CallButton.yaml
  </files>
  <read_first>
    - examples/elevator/.design/model/Elevator/state-diagrams/Elevator.yaml (read entire file)
    - examples/elevator/.design/model/Elevator/state-diagrams/ElevatorCall.yaml (read entire file)
    - examples/elevator/.design/model/Elevator/state-diagrams/FloorCall.yaml (read entire file)
    - examples/elevator/.design/model/Elevator/state-diagrams/Request.yaml (read entire file)
    - examples/elevator/.design/model/Elevator/state-diagrams/CallButton.yaml (read entire file)
    - examples/elevator/.design/model/Elevator/class-diagram.yaml (associations and attributes)
    - pycca/SYNTAX.md (canonical syntax reference)
    - schema/COMPILATION.md (referential naming, subtype inheritance)
    - .planning/phases/04.1-model-development-and-compiler-testing/04.1-04-SUMMARY.md (grammar syntax decisions)
  </read_first>
  <action>
Process each file in this order. For each file: read it, identify all if-stmt style
mismatches and syntax errors, then update to the canonical brace-style pycca.

ELEVATOR.YAML:

The Idle, Moving_Up, Moving_Down, Stopping, At_Floor states have entry actions from
earlier plans (Plans 03 already corrected At_Floor). Review each entry action:

Idle state entry_action — current version uses old if syntax with semicolons and
bare class name "Dispatcher". Fix to:
  entry_action: >-
    self.direction = Stopped;
    self.next_stop_floor = 0;
    select any req from instances of Request where request_id == self.r14_request_id;
    if (req != empty) {
      generate Request_assigned(target_floor: req.destination_floor) to self;
    } else {
      select any disp from instances of Dispatcher;
      generate Elevator_available(queue_length: 0) to disp;
    }

Moving_Up state entry_action — current uses old "select any shaft from instances of
Shaft where R11" syntax. Fix to:
  entry_action: >-
    self.direction = Up;
    select any shaft related by self->R11;
    generate Move_up to shaft;

Moving_Down state entry_action — same pattern as Moving_Up:
  entry_action: >-
    self.direction = Down;
    select any shaft related by self->R11;
    generate Move_down to shaft;

Stopping state entry_action — same pattern (generate Stop to shaft):
  entry_action: >-
    self.direction = Stopped;
    select any shaft related by self->R11;
    generate Stop to shaft;

At_Floor entry_action — was corrected in Plan 03. Review and confirm it uses brace-style
if syntax matching Plan 04 grammar. Update if syntax style if needed.

Transition actions that use guard variables (target_floor, current_floor) — these are
rcvd_evt parameters accessed by short name in guards (the guard parser is more permissive).
The transition action "self.next_stop_floor = target_floor;" should become:
  "self.next_stop_floor = rcvd_evt.target_floor;"
Check all transition actions and add rcvd_evt. prefix where needed.

ELEVATORCALL.YAML:

Fulfilled state entry_action: "generate Call_fulfilled to r7_button_id;"
This references r7_button_id which is inherited from the Call supertype (ELV-001 fix in Plan 02).
Per pycca self. prefix rules, this becomes: generate Call_fulfilled to self.r7_button_id;
Update to: entry_action: "generate Call_fulfilled to self.r7_button_id;"

FLOORCALL.YAML:

Fulfilled state entry_action references r7_button_id and r8_request_id (fixed in Plan 03).
Review and confirm it uses self. prefix and brace-style if:
  entry_action: >-
    generate Call_fulfilled to self.r7_button_id;
    select any req from instances of Request where request_id == self.r8_request_id;
    if (req != empty) { generate Request_cancelled to req; }

REQUEST.YAML:

Complete state entry_action: "generate Service_complete to r2_elevator_id;"
This is a referential on Request. Per pycca self. prefix:
  entry_action: "generate Service_complete to self.r2_elevator_id;"

Review all other states for any missing action bodies. Add a Serving state entry_action
if there is meaningful behavior (e.g., unrelating from previous head-pointer position).
For now, if no clear behavior is needed, leave Serving without an entry_action.

CALLBUTTON.YAML:

Button_pushed transition action was fixed in Plan 03: "create ec of ElevatorCall;"
This also needs to relate ec to self and relate to the Dispatcher. The minimal correct
behavior per SYNTAX.md:
  action: >-
    create ec of ElevatorCall;
    relate ec to self across R7;
    select any disp from instances of Dispatcher;
    generate Call_submitted(has_idle_elevator: disp.has_idle_elevator) to disp;

If R7 goes from CallButton to Call (not directly to ElevatorCall), the relate may need
to relate through the Call supertype. Use the simpler form unless the schema requires it:
  action: >-
    create ec of ElevatorCall;
    select any disp from instances of Dispatcher;
    generate Call_submitted(has_idle_elevator: disp.has_idle_elevator) to disp;

For all files: after updating action bodies, verify each body string parses cleanly with
STATEMENT_PARSER. Run the pycca parser inline:
  python -c "from pycca.grammar import STATEMENT_PARSER; STATEMENT_PARSER.parse('body here'); print('OK')"
Use this to test each body before moving to the next.
  </action>
  <verify>
    <automated>pytest tests/test_pycca.py tests/test_validation.py -x -q 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep "where R11" examples/elevator/.design/model/Elevator/state-diagrams/Elevator.yaml returns zero matches
    - grep "to Dispatcher;" examples/elevator/.design/model/Elevator/state-diagrams/Elevator.yaml returns zero matches
    - grep "to SELF;" examples/elevator/.design/model/Elevator/state-diagrams/Elevator.yaml returns zero matches
    - grep "end if;" examples/elevator/.design/model/Elevator/state-diagrams/Elevator.yaml returns zero matches (old syntax gone)
    - grep "related by self->R11" examples/elevator/.design/model/Elevator/state-diagrams/Elevator.yaml returns 3 matches (Moving_Up, Moving_Down, Stopping)
    - grep "self.r7_button_id" examples/elevator/.design/model/Elevator/state-diagrams/ElevatorCall.yaml returns 1 match
    - grep "self.r2_elevator_id" examples/elevator/.design/model/Elevator/state-diagrams/Request.yaml returns 1 match
    - pytest tests/test_validation.py -x -q exits 0
  </acceptance_criteria>
  <done>All five YAML files have correct brace-style pycca action bodies with proper self. and rcvd_evt. prefixes</done>
</task>

<task type="auto">
  <name>Task 2: Complete Dispatcher.yaml action bodies and write tests/test_elevator.py</name>
  <files>
    examples/elevator/.design/model/Elevator/state-diagrams/Dispatcher.yaml,
    tests/test_elevator.py
  </files>
  <read_first>
    - examples/elevator/.design/model/Elevator/state-diagrams/Dispatcher.yaml (read entire file)
    - tools/validation.py (validate_model signature and monkeypatch.chdir pattern)
    - tests/test_validation.py (read for monkeypatch.chdir usage pattern)
    - examples/elevator/README.md (if exists — for elevator domain overview)
  </read_first>
  <action>
DISPATCHER.YAML — Review and complete action bodies.

The Dispatcher transition actions were updated in Plan 03 with pragmatic select from
instances of FloorCall patterns. Review them now:
  - Confirm brace-style if syntax is used
  - Confirm create var of Request syntax
  - Confirm generate Request_assigned uses rcvd_evt form for params if applicable
  - Add self. prefix where referential attributes are accessed

The Idle and Waiting states have no entry_action. Add entry actions if meaningful:
  Idle entry_action — set has_idle_elevator = True:
    entry_action: "self.has_idle_elevator = True;"

  Waiting entry_action — update queue_length counter (increment):
    No clear incrementing logic available without formal Dispatcher-FloorCall association.
    Leave as no entry_action for now (Claude's discretion — ELV-007 workaround).

TESTS/TEST_ELEVATOR.PY — Write end-to-end integration test.

Create tests/test_elevator.py with these tests:

1. test_elevator_model_clean:
   Uses monkeypatch.chdir to change CWD to examples/elevator directory.
   Calls validate_model("Elevator").
   Asserts the returned list is empty (issues == []).
   If issues exist, prints them for debugging before the assertion fails.

2. test_elevator_schema_loads:
   Uses monkeypatch.chdir to examples/elevator.
   Calls read_model("Elevator") and confirms no schema errors are returned.
   (Uses the existing model_io.read_model function.)

Import pattern:
  import pytest
  from tools.validation import validate_model
  from tools.model_io import read_model

Fixture for chdir:
  @pytest.fixture(autouse=False)
  def elevator_root(monkeypatch):
      monkeypatch.chdir("examples/elevator")

Each test receives elevator_root as a parameter to trigger the fixture.

IMPORTANT: Before writing test_elevator.py, run validate_model manually to see the
current issue list and confirm it is empty (or fix remaining issues):
  cd examples/elevator
  python -c "from tools.validation import validate_model; issues = validate_model('Elevator'); print(f'{len(issues)} issues'); [print(i) for i in issues]"

If issues remain, fix them in the state diagram YAML files before writing the test.
The test must pass on first run — do not write a test that is known to fail.
  </action>
  <verify>
    <automated>pytest tests/test_elevator.py tests/test_pycca.py tests/test_validation.py -x -q 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_elevator.py exists and contains def test_elevator_model_clean
    - grep "monkeypatch.chdir" tests/test_elevator.py returns at least 1 match
    - grep "validate_model" tests/test_elevator.py returns at least 1 match
    - pytest tests/test_elevator.py -x -q exits 0 with all tests passing
    - pytest tests/ -x -q exits 0 (full suite green)
    - Manually running: cd examples/elevator && python -c "from tools.validation import validate_model; issues = validate_model('Elevator'); assert issues == [], issues" exits 0
  </acceptance_criteria>
  <done>test_elevator.py passes; validate_model('Elevator') returns zero issues; full test suite green</done>
</task>

</tasks>

<verification>
Final phase gate check:
  cd examples/elevator
  python -c "from tools.validation import validate_model; issues = validate_model('Elevator'); print(f'{len(issues)} issues remaining'); [print(i) for i in issues]"

Must print "0 issues remaining".

Full test suite:
  pytest tests/ -x -q 2>&amp;1 | tail -10
Must exit 0.
</verification>

<success_criteria>
- validate_model("Elevator") returns [] (zero issues)
- pytest tests/test_elevator.py -x -q exits 0
- pytest tests/ -x -q exits 0
- No action body in the elevator model contains: "create object of", "to SELF;", "to CLASS;", "end if;", bare class name as generate target
- All five issue files (ELV-001 through ELV-007) have Fix Applied sections completed
</success_criteria>

<output>
After completion, create .planning/phases/04.1-model-development-and-compiler-testing/04.1-06-SUMMARY.md
</output>
