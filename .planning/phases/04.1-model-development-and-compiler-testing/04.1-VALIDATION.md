---
phase: 4.1
slug: model-development-and-compiler-testing
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-03-16
---

# Phase 4.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest |
| **Config file** | pytest.ini or pyproject.toml |
| **Quick run command** | `python -m pytest tests/test_yaml_schema.py tests/test_pycca.py -x -q` |
| **Full suite command** | `python -m pytest tests/ -x -q` |
| **Estimated runtime** | ~10 seconds |

---

## Sampling Rate

- **After every task commit:** Run `python -m pytest tests/test_yaml_schema.py tests/test_pycca.py -x -q`
- **After every plan wave:** Run `python -m pytest tests/ -x -q`
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 15 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| schema-fix | TBD | 1 | schema integrity | unit | `python -m pytest tests/test_yaml_schema.py -x -q` | ✅ | ⬜ pending |
| grammar-ext | TBD | 2 | grammar coverage | unit | `python -m pytest tests/test_pycca.py -x -q` | ❌ W0 | ⬜ pending |
| model-complete | TBD | 3 | elevator model | integration | `python -m pytest tests/test_elevator.py -x -q` | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `tests/test_elevator.py` — elevator model validation stubs
- [ ] `tests/test_pycca.py` — pycca grammar test stubs (may already exist)

*If none: "Existing infrastructure covers all phase requirements."*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Action body semantic correctness | model completeness | Grammar parses but semantics require human review | Read each state YAML and verify action bodies match intended behavior |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 15s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
