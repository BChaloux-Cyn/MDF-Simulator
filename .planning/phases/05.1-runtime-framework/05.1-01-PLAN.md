---
phase: 05.1-runtime-framework
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - engine/__init__.py
  - engine/microstep.py
  - engine/event.py
  - engine/manifest.py
  - tests/test_engine.py
  - tests/fixtures/bridge_mocks.yaml
autonomous: true
requirements: [SC-08, SC-11]
must_haves:
  truths:
    - "All 12 micro-step dataclasses are importable from engine.microstep"
    - "Event dataclass carries type, sender_id, target_id, args, delay_ms fields"
    - "Domain manifest spec defines class_defs, associations, generalization hierarchy, final_states, transition_tables"
    - "Test file has stubs covering all 11 success criteria"
    - "Bridge mock fixture YAML file exists in tests/fixtures/"
  artifacts:
    - path: "engine/microstep.py"
      provides: "All 12 micro-step dataclasses + ErrorMicroStep"
      exports: ["MicroStep", "SchedulerSelected", "EventReceived", "GuardEvaluated", "TransitionFired", "ActionExecuted", "GenerateDispatched", "EventDelayed", "EventDelayExpired", "EventCancelled", "InstanceCreated", "InstanceDeleted", "BridgeCalled", "ErrorMicroStep"]
    - path: "engine/event.py"
      provides: "Event dataclass and make_instance_key helper"
      exports: ["Event", "make_instance_key"]
    - path: "engine/manifest.py"
      provides: "Domain manifest type spec as TypedDicts"
      exports: ["DomainManifest", "ClassManifest", "AssociationManifest", "TransitionEntry"]
    - path: "tests/test_engine.py"
      provides: "Test stubs for all 11 success criteria"
      min_lines: 80
    - path: "tests/fixtures/bridge_mocks.yaml"
      provides: "Bridge mock fixture for test harness"
  key_links:
    - from: "engine/microstep.py"
      to: "engine/event.py"
      via: "micro-step fields reference Event fields"
      pattern: "from_state|to_state|event_type"
---

<objective>
Define all shared data types for the runtime framework: micro-step dataclasses (all 12 types per D-26 + error variant per D-28), Event dataclass (per D-22), domain manifest TypedDict spec, and instance key helper. Create the Wave 0 test scaffold with stubs for all 11 success criteria and the bridge mock fixture.

Purpose: Every subsequent plan imports these types. Defining contracts first prevents the "scavenger hunt" anti-pattern.
Output: engine/microstep.py, engine/event.py, engine/manifest.py, tests/test_engine.py (stubs), tests/fixtures/bridge_mocks.yaml
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-runtime-framework/05.1-CONTEXT.md
@.planning/phases/05.1-runtime-framework/05.1-RESEARCH.md
@docs/2026-03-06-simulation-engine-design.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Micro-step dataclasses, Event, instance key helper, domain manifest spec</name>
  <files>engine/microstep.py, engine/event.py, engine/manifest.py, engine/__init__.py</files>
  <read_first>
    - engine/__init__.py (current stub — will be replaced with re-exports)
    - docs/2026-03-06-simulation-engine-design.md (micro-step type table at line ~114)
    - .planning/phases/05.1-runtime-framework/05.1-CONTEXT.md (D-22, D-26, D-27, D-28)
    - .planning/phases/05.1-runtime-framework/05.1-RESEARCH.md (code examples section)
  </read_first>
  <behavior>
    - Test: all 12 micro-step classes are importable from engine.microstep
    - Test: each micro-step has a `type` field matching its Literal string
    - Test: SchedulerSelected.type == "scheduler_selected", EventReceived.type == "event_received", etc.
    - Test: ErrorMicroStep has type="error", error_kind str, and message str fields
    - Test: Event has event_type, sender_class, sender_id, target_class, target_id, args, delay_ms fields
    - Test: make_instance_key({"floor_num": 3}) returns frozenset({("floor_num", 3)})
    - Test: make_instance_key({"a": 1, "b": 2}) is hashable and order-independent
    - Test: DomainManifest TypedDict has keys: class_defs, associations, generalizations
    - Test: TransitionEntry TypedDict has keys: next_state, action_fn, guard_fn
  </behavior>
  <action>
Create `engine/microstep.py` with all 12 micro-step dataclasses per D-26 plus an ErrorMicroStep per D-28. Each is a frozen dataclass inheriting from a base `MicroStep` dataclass. The `type` field is a `Literal` string set via `field(default=..., init=False)`. Specific fields per type:

- `SchedulerSelected`: queue (Literal["priority","standard"]), event_type (str), target_class (str), target_instance_id (dict)
- `EventReceived`: class_name (str), instance_id (dict), event_type (str), args (dict), queue (Literal["priority","standard"])
- `GuardEvaluated`: expression (str), result (bool), variable_values (dict)
- `TransitionFired`: from_state (str), to_state (str), class_name (str), instance_id (dict)
- `ActionExecuted`: pycca_line (str), assignments_made (dict)
- `GenerateDispatched`: event_type (str), sending_class (str), sending_instance_id (dict), target_class (str), target_instance_id (dict), args (dict), queue (Literal["priority","standard","delay"])
- `EventDelayed`: event_type (str), sending_class (str), sending_instance_id (dict), target_class (str), target_instance_id (dict), args (dict), delay_ms (float)
- `EventDelayExpired`: event_type (str), target_class (str), target_instance_id (dict)
- `EventCancelled`: cancelled_event_type (str), sender_id (dict), target_id (dict)
- `InstanceCreated`: class_name (str), instance_id (dict), initial_attrs (dict), mode (Literal["sync","async"])
- `InstanceDeleted`: class_name (str), instance_id (dict), mode (Literal["sync","async"])
- `BridgeCalled`: operation (str), args (dict), mock_return (Any)
- `ErrorMicroStep`: error_kind (str), message (str), context (dict)

Create `engine/event.py` with:
- `Event` dataclass: event_type (str), sender_class (str), sender_id (dict), target_class (str), target_id (dict), args (dict[str,Any], default_factory=dict), delay_ms (float|None, default=None)
- `make_instance_key(identifier_attrs: dict) -> frozenset` — converts `{attr: value}` to `frozenset(sorted(identifier_attrs.items()))` for use as hashable dict key. Sort ensures order-independence.

Create `engine/manifest.py` with TypedDict definitions (using `typing.TypedDict`):
- `TransitionEntry`: next_state (str|None), action_fn (Callable|None), guard_fn (Callable|None)
- `ClassManifest`: name (str), is_abstract (bool), identifier_attrs (list[str]), attributes (dict[str,Any]), initial_state (str|None), final_states (list[str]), transition_table (dict[tuple[str,str], TransitionEntry]), supertype (str|None), subtypes (list[str])
- `AssociationManifest`: rel_id (str), class_a (str), class_b (str), mult_a_to_b (str), mult_b_to_a (str)
- `DomainManifest`: class_defs (dict[str, ClassManifest]), associations (dict[str, AssociationManifest]), generalizations (dict[str, list[str]])

Update `engine/__init__.py` to re-export the public surface:
```python
"""engine - MDF simulation engine runtime framework."""
from engine.microstep import (
    MicroStep, SchedulerSelected, EventReceived, GuardEvaluated,
    TransitionFired, ActionExecuted, GenerateDispatched, EventDelayed,
    EventDelayExpired, EventCancelled, InstanceCreated, InstanceDeleted,
    BridgeCalled, ErrorMicroStep,
)
from engine.event import Event, make_instance_key
```

CRITICAL per D-37: No imports from schema/, tools/, or pycca/ anywhere in engine/.
  </action>
  <verify>
    <automated>.venv/Scripts/python -m pytest tests/test_engine.py::test_microstep_types -x -q</automated>
  </verify>
  <done>
    - All 12 micro-step dataclasses importable from engine.microstep
    - Each has correct Literal type field
    - ErrorMicroStep has error_kind, message, context fields
    - Event dataclass has all 7 fields
    - make_instance_key returns order-independent frozenset
    - DomainManifest, ClassManifest, AssociationManifest, TransitionEntry TypedDicts defined
    - engine/__init__.py re-exports micro-steps and Event
    - Zero imports from schema/, tools/, or pycca/ in any engine/ file
  </done>
  <acceptance_criteria>
    - engine/microstep.py contains `class SchedulerSelected(MicroStep):`
    - engine/microstep.py contains `class ErrorMicroStep(MicroStep):`
    - engine/microstep.py contains `type: Literal["scheduler_selected"]` (and all 11 other type literals)
    - engine/event.py contains `class Event:` and `def make_instance_key(`
    - engine/event.py contains `frozenset(sorted(` 
    - engine/manifest.py contains `class DomainManifest(TypedDict):`
    - engine/manifest.py contains `class TransitionEntry(TypedDict):`
    - engine/__init__.py contains `from engine.microstep import`
    - `grep -r "from schema\|from tools\|from pycca" engine/` returns empty
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Wave 0 test scaffold + bridge mock fixture + hand-written domain manifest fixture</name>
  <files>tests/test_engine.py, tests/fixtures/bridge_mocks.yaml</files>
  <read_first>
    - tests/test_pycca.py (established test style — pytest functions, no classes, direct imports)
    - tests/fixtures/ (existing fixture directory)
    - .planning/phases/05.1-runtime-framework/05.1-VALIDATION.md (all 11 SC mappings)
    - .planning/phases/05.1-runtime-framework/05.1-RESEARCH.md (open questions section — manifest structure, transition table representation)
  </read_first>
  <action>
Create `tests/test_engine.py` with:

1. A `DOMAIN_MANIFEST` dict fixture at module top level — a hand-written manifest for a minimal two-class model (e.g., "TrafficLight" with states Idle/Green/Yellow/Red and "Controller" with states Off/Running). The manifest must conform to the DomainManifest TypedDict from engine/manifest.py. Include:
   - TrafficLight: identifier_attrs=["light_id"], is_abstract=False, initial_state="Idle", final_states=[], transition_table with entries for (Idle,TurnOn)->(Green,action_fn,None), (Green,Timer)->(Yellow,...), (Yellow,Timer)->(Red,...), (Red,Timer)->(Green,...). Action functions are plain Python lambdas or functions defined in the test file.
   - Controller: identifier_attrs=["ctrl_id"], is_abstract=False, initial_state="Off", final_states=["Shutdown"], transition_table with entries for (Off,Start)->(Running,...), (Running,Stop)->(Shutdown,...). Shutdown is a final state (no outgoing transitions).
   - One association: R1 between Controller(1) and TrafficLight(M)
   - No generalizations in this minimal fixture

2. A `BRIDGE_MOCKS` dict: {"LogEvent": "ok", "GetTimeOfDay": "12:00"}

3. Test stubs with `@pytest.mark.skip(reason="Implemented in plan 05.1-NN")` for each SC:
   - `test_registry_create_sync_delete_lookup` (SC-01) — skip, plan 05.1-02
   - `test_registry_create_async` (SC-01) — skip, plan 05.1-02
   - `test_registry_composite_identifier` (SC-01) — skip, plan 05.1-02
   - `test_relationships_relate_unrelate_navigate` (SC-02) — skip, plan 05.1-02
   - `test_relationships_multiplicity_enforcement` (SC-02) — skip, plan 05.1-02
   - `test_relationships_chained_navigation` (SC-02) — skip, plan 05.1-02
   - `test_scheduler_priority_before_standard` (SC-03) — skip, plan 05.1-04
   - `test_scheduler_fifo_within_queue` (SC-03) — skip, plan 05.1-04
   - `test_scheduler_delay_feeds_standard` (SC-03) — skip, plan 05.1-04
   - `test_run_to_completion` (SC-04) — skip, plan 05.1-04
   - `test_polymorphic_dispatch` (SC-05) — skip, plan 05.1-04
   - `test_cant_happen_error_microstep` (SC-06) — skip, plan 05.1-04
   - `test_event_ignored_silent` (SC-06) — skip, plan 05.1-04
   - `test_final_state_async_deletion` (SC-07) — skip, plan 05.1-04
   - `test_all_microstep_types` (SC-08) — skip, plan 05.1-05
   - `test_bridge_mock_hit` (SC-09) — skip, plan 05.1-03
   - `test_bridge_mock_miss_returns_none` (SC-09) — skip, plan 05.1-03
   - `test_determinism_identical_streams` (SC-10) — skip, plan 05.1-05
   - `test_engine_isolation_no_schema_imports` (SC-11) — implemented immediately (not skipped)

4. One IMPLEMENTED test: `test_engine_isolation_no_schema_imports` — uses `pathlib.Path` to glob all `*.py` files in `engine/`, reads each, asserts none contain `from schema`, `from tools`, `from pycca`, `import schema`, `import tools`, `import pycca`.

5. One IMPLEMENTED test: `test_microstep_types` — imports all 12 micro-step classes from `engine.microstep`, verifies each has the correct `.type` Literal value, verifies `ErrorMicroStep` exists with `error_kind` field.

6. One IMPLEMENTED test: `test_event_and_instance_key` — imports `Event` and `make_instance_key` from `engine.event`, creates an Event, verifies fields, verifies `make_instance_key({"a":1,"b":2}) == make_instance_key({"b":2,"a":1})`.

Create `tests/fixtures/bridge_mocks.yaml`:
```yaml
LogEvent: "ok"
GetTimeOfDay: "12:00"
IsTopFloor: true
IsBottomFloor: false
```

  </action>
  <verify>
    <automated>.venv/Scripts/python -m pytest tests/test_engine.py -x -q</automated>
  </verify>
  <done>
    - tests/test_engine.py exists with 19+ skipped stubs and 3 passing tests
    - test_engine_isolation_no_schema_imports passes (SC-11)
    - test_microstep_types passes (SC-08 partial)
    - test_event_and_instance_key passes
    - All skipped tests reference the plan that will implement them
    - tests/fixtures/bridge_mocks.yaml exists with 4 mock operations
    - DOMAIN_MANIFEST dict defined in test file
  </done>
  <acceptance_criteria>
    - tests/test_engine.py contains `DOMAIN_MANIFEST`
    - tests/test_engine.py contains `def test_engine_isolation_no_schema_imports`
    - tests/test_engine.py contains `def test_microstep_types`
    - tests/test_engine.py contains `def test_event_and_instance_key`
    - tests/test_engine.py contains `@pytest.mark.skip(reason=` at least 19 times
    - tests/test_engine.py contains `def test_registry_create_sync_delete_lookup`
    - tests/test_engine.py contains `def test_scheduler_priority_before_standard`
    - tests/test_engine.py contains `def test_determinism_identical_streams`
    - tests/fixtures/bridge_mocks.yaml contains `LogEvent:`
    - `.venv/Scripts/python -m pytest tests/test_engine.py -x -q` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This plan creates data types and test scaffolds only — no input parsing or external data loading |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5.1-01 | Denial of Service | bridge_mocks.yaml | accept | Fixture file is test-only; no user input. yaml.safe_load() enforced in Plan 03 when registry loads it. |
</threat_model>

<verification>
- `.venv/Scripts/python -m pytest tests/test_engine.py -x -q` — 3 pass, 19+ skip, 0 fail
- `python -c "from engine.microstep import SchedulerSelected; print(SchedulerSelected.__dataclass_fields__)"` — prints fields
- `python -c "from engine.event import make_instance_key; print(make_instance_key({'a':1}))"` — prints frozenset
- `grep -r "from schema\|from tools\|from pycca" engine/` — returns empty
</verification>

<success_criteria>
All 12 micro-step types + ErrorMicroStep defined as frozen dataclasses. Event and make_instance_key helper defined. DomainManifest TypedDict spec defined. Test scaffold with 19+ skipped stubs and 3 passing tests. Bridge mock fixture exists. Zero schema/tools/pycca imports in engine/.
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-runtime-framework/05.1-01-SUMMARY.md`
</output>
