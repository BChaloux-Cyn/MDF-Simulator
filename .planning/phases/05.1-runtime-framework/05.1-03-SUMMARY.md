---
phase: 05.1-runtime-framework
plan: 03
subsystem: engine
tags: [engine, runtime, clock, bridge, mocks]
requires:
  - engine.microstep.BridgeCalled
provides:
  - engine.clock.SimulationClock
  - engine.bridge.BridgeMockRegistry
affects: [engine/, tests/]
tech-stack:
  added: []
  patterns:
    - simulation time tracked as float ms, decoupled from wall clock
    - speed_multiplier scales advance() delta only (delay-queue scope per D-32)
    - YAML mock registry loaded via yaml.safe_load() per T-5.1-01
    - bridge.call() always returns a BridgeCalled micro-step (even on miss, D-30)
key-files:
  created:
    - engine/clock.py
    - engine/bridge.py
  modified:
    - tests/test_engine.py
decisions:
  - SimulationClock.advance() is a no-op while paused — pause() does not record wall-clock at pause time, since the clock is purely virtual
  - BridgeMockRegistry.from_yaml raises ValueError if the YAML root is not a mapping — keeps the constructor invariant `_mocks: dict` total
  - BridgeMockRegistry copies the input mocks dict in __init__ to prevent external mutation of the registry after construction
metrics:
  duration_min: 4
  tasks: 2
  files_changed: 3
  completed: 2026-04-06
requirements: [SC-09]
---

# Phase 05.1 Plan 03: Simulation Clock and Bridge Mock Registry Summary

Built the two leaf-level runtime helpers — `SimulationClock` (virtual time with pause/resume + speed multiplier) and `BridgeMockRegistry` (YAML-loaded bridge stubs that always emit a `BridgeCalled` micro-step). These are the last two pieces the scheduler (plan 04) and ctx (plan 05) need before they can be wired together.

## What Shipped

- `engine/clock.py` — `SimulationClock`
  - `now() -> float` returns sim time in ms (D-34)
  - `advance(delta_ms)` adds `delta_ms * speed_multiplier`; no-op when paused (D-32, D-33)
  - `pause()` / `resume()` toggle the pause flag
  - `paused` and `speed_multiplier` properties (multiplier is mutable via setter)
- `engine/bridge.py` — `BridgeMockRegistry`
  - `__init__(mocks=None)` accepts a plain dict; copies it defensively
  - `from_yaml(path)` classmethod uses `yaml.safe_load()` only (T-5.1-01); rejects non-mapping roots with `ValueError`
  - `call(operation, args)` returns `(mock_return, BridgeCalled)`; unknown ops return `(None, BridgeCalled(mock_return=None))` per D-30
- `tests/test_engine.py`
  - 3 new clock tests: `test_clock_basic`, `test_clock_speed_multiplier`, `test_clock_pause_resume`
  - 2 unskipped bridge tests: `test_bridge_mock_hit` (loads fixture, exercises both `LogEvent` and `GetTimeOfDay`), `test_bridge_mock_miss_returns_none`

## Verification

`.venv/Scripts/python -m pytest tests/test_engine.py -k "test_clock or test_bridge_mock" -x -q` -> **5 passed**.

`.venv/Scripts/python -m pytest tests/test_engine.py -x -q` -> **19 passed, 23 skipped** (was 14 passed / 25 skipped — +3 clock tests + 2 unskipped bridge tests, no regressions).

`grep -n "yaml.load(" engine/bridge.py` -> only matches inside comments documenting the T-5.1-01 prohibition; no live `yaml.load()` call. T-5.1-01 mitigated.

`grep -E "from (schema|tools|pycca)" engine/clock.py engine/bridge.py` -> empty (SC-11 satisfied).

## Commits

- `3e21a6d` test(05.1-03): add failing tests for SimulationClock
- `413c66a` feat(05.1-03): implement SimulationClock with pause/resume and speed multiplier
- `ff112dc` test(05.1-03): unskip bridge mock tests with real assertions
- `476ded4` feat(05.1-03): implement BridgeMockRegistry with safe YAML loading

## Deviations from Plan

None. Both tasks executed exactly as written, in strict TDD (RED commit -> GREEN commit). No Rule 1/2/3 fixes triggered; no Rule 4 architectural questions.

The plan listed `engine/clock.py`, `engine/bridge.py`, and `tests/test_engine.py` as the only files modified — final result matches.

## Known Stubs

None. Both new components have full behavior; the still-skipped tests in `tests/test_engine.py` belong to plans 05.1-04 and 05.1-05 and are correctly labeled in their skip reasons.

## Self-Check: PASSED

- engine/clock.py — FOUND
- engine/bridge.py — FOUND
- tests/test_engine.py — FOUND (modified)
- commit 3e21a6d — FOUND
- commit 413c66a — FOUND
- commit ff112dc — FOUND
- commit 476ded4 — FOUND
