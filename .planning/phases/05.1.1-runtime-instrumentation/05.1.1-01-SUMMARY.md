---
phase: 05.1.1-runtime-instrumentation
plan: 01
subsystem: engine
tags: [dataclass, microstep, manifest, simulation, instrumentation]

# Dependency graph
requires:
  - phase: 05.1-runtime-framework
    provides: ThreeQueueScheduler, SimulationContext, micro-step dataclass pattern, 42 engine tests
provides:
  - EventCompleted frozen dataclass in engine/microstep.py
  - LongEventWarning frozen dataclass in engine/microstep.py
  - SenescentEntered frozen dataclass in engine/microstep.py
  - SenescentExited frozen dataclass in engine/microstep.py
  - ClassManifest.senescent_states NotRequired field in engine/manifest.py
  - SimulationContext.event_duration_warn_ns attribute (default None) in engine/ctx.py
  - Re-exports for all 4 new types in engine/__init__.py
  - 10 RED test stubs for D-22.1..D-22.10 in tests/test_engine_instrumentation.py
affects:
  - 05.1.1-02 (scheduler instrumentation — consumes EventCompleted, LongEventWarning)
  - 05.1.1-03 (ctx LongEventWarning wrapper — reads event_duration_warn_ns)
  - 05.2-model-compiler (populates senescent_states in ClassManifest)
  - 05.6-metrics-gatherer (consumes SenescentEntered/Exited for dwell-time histograms)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "New micro-step types follow @dataclass(frozen=True) pattern with Literal type field"
    - "NotRequired TypedDict field for backward-compatible optional manifest keys"
    - "RED test stubs use pytest.fail() so they collect and fail, not skip"

key-files:
  created:
    - tests/test_engine_instrumentation.py
  modified:
    - engine/microstep.py
    - engine/manifest.py
    - engine/ctx.py
    - engine/__init__.py

key-decisions:
  - "senescent_states uses NotRequired[set[str]] in ClassManifest — existing manifests without the field remain valid; access via .get('senescent_states') or set()"
  - "event_duration_warn_ns is a plain attribute (int | None = None) — no getter/setter, no config abstraction, per D-24"
  - "10 RED stubs use pytest.fail() not pytest.skip() — proves test scaffold is in place at Wave 0"

patterns-established:
  - "Phase 5.1.1 test file: tests/test_engine_instrumentation.py — separate from tests/test_engine.py per scale guidance in D-22"
  - "Instrumentation micro-steps appended to end of microstep.py, preserving all 12 original types untouched"

requirements-completed: [MCP-08]

# Metrics
duration: 3min
completed: 2026-04-06
---

# Phase 05.1.1 Plan 01: Runtime Instrumentation Foundation Summary

**4 frozen micro-step dataclasses (EventCompleted, LongEventWarning, SenescentEntered, SenescentExited) added to engine with NotRequired manifest field and opt-in ctx attribute, plus 10 RED test stubs establishing the D-22 baseline**

## Performance

- **Duration:** ~3 min
- **Started:** 2026-04-06T17:24:26Z
- **Completed:** 2026-04-06T17:27:30Z
- **Tasks:** 2
- **Files modified:** 5

## Accomplishments

- Added 4 new frozen dataclasses to `engine/microstep.py` following the exact existing pattern — `EventCompleted`, `LongEventWarning`, `SenescentEntered`, `SenescentExited`
- Extended `ClassManifest` with `senescent_states: NotRequired[set[str]]` — backward compatible, existing 42 tests unaffected
- Added `event_duration_warn_ns: int | None = None` attribute to `SimulationContext.__init__` per D-24 (no methods, no logic)
- Re-exported all 4 new types in `engine/__init__.py` import block and `__all__`
- Created `tests/test_engine_instrumentation.py` with exactly 10 named RED stubs for D-22.1..D-22.10

## Task Commits

1. **Task 1: Add 4 new micro-step dataclasses + manifest field + ctx attribute + re-exports** — `2dcd020` (feat)
2. **Task 2: Create 10 failing test stubs for D-22.1..D-22.10** — `36dad35` (test)

**Plan metadata:** (docs commit follows)

## Files Created/Modified

- `engine/microstep.py` — 4 new frozen dataclasses appended after ErrorMicroStep
- `engine/manifest.py` — NotRequired imported; senescent_states field added as last ClassManifest key
- `engine/ctx.py` — event_duration_warn_ns: int | None = None added after self._running = False in __init__
- `engine/__init__.py` — 4 new names added to import block and __all__
- `tests/test_engine_instrumentation.py` — NEW: 10 RED stubs for D-22.1..D-22.10

## Decisions Made

- Used `NotRequired[set[str]]` (not `set[str]` with a default) for `senescent_states` — TypedDict fields cannot have runtime defaults, so `NotRequired` is the correct approach. Callers use `.get("senescent_states") or set()`.
- Kept `event_duration_warn_ns` as a bare attribute with no property wrapper — plan explicitly prohibits adding any getter/setter per D-24.
- Set `.venv` up via `uv venv` + `uv pip install -r requirements.txt` as the environment was absent from the working tree at execution time (not tracked in git, as expected).

## Deviations from Plan

**1. [Rule 3 - Blocking] Created missing .venv**
- **Found during:** Task 1 verification
- **Issue:** `.venv/` not present (not git-tracked, expected); pytest invocation from COMMANDS.md failed with "No such file or directory"
- **Fix:** Ran `uv venv .venv && uv pip install -r requirements.txt && uv pip install pytest`
- **Files modified:** None (runtime environment only, not committed)
- **Verification:** `python -m pytest tests/test_engine.py -x -q` → 42 passed

---

**Total deviations:** 1 auto-fixed (blocking — missing venv)
**Impact on plan:** Required to run verification. No scope creep.

## Issues Encountered

None beyond the blocking venv setup above.

## Known Stubs

None — this plan's goal is to establish types and test scaffolding. The 10 test stubs in `tests/test_engine_instrumentation.py` are intentional RED placeholders. Plans 02 and 03 replace each body with real assertions.

## Threat Flags

None — no new network endpoints, auth paths, file access patterns, or schema changes at trust boundaries introduced.

## Self-Check: PASSED

- `engine/microstep.py` — FOUND (4 new dataclasses appended)
- `engine/manifest.py` — FOUND (NotRequired import + senescent_states field)
- `engine/ctx.py` — FOUND (event_duration_warn_ns attribute)
- `engine/__init__.py` — FOUND (8 new references confirmed by grep -c >= 8)
- `tests/test_engine_instrumentation.py` — FOUND (10 stubs, 10 FAILED)
- Commit `2dcd020` — FOUND
- Commit `36dad35` — FOUND
- 42 tests/test_engine.py passing — CONFIRMED

## Next Phase Readiness

- Plan 02 (scheduler instrumentation) can now import `EventCompleted`, `LongEventWarning`, `SenescentEntered`, `SenescentExited` from `engine` and implement the dispatch-loop hooks
- Plan 03 (ctx LongEventWarning wrapper) can read `ctx.event_duration_warn_ns` to gate warning emission
- `senescent_states` field ready for consumption by scheduler (Plan 02) and future population by Phase 5.2 compiler

---
*Phase: 05.1.1-runtime-instrumentation*
*Completed: 2026-04-06*
