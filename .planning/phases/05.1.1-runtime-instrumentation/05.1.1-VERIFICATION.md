---
phase: 05.1.1-runtime-instrumentation
verified: 2026-04-06T18:00:00Z
status: passed
score: 9/9 must-haves verified
---

# Phase 05.1.1: Runtime Instrumentation Verification Report

**Phase Goal:** Add runtime observability micro-steps to the engine: `EventCompleted` with `duration_ns`, opt-in `LongEventWarning` threshold, `SenescentEntered`/`SenescentExited`, plus a `senescent_states: set[str]` field on `ClassManifest`. Pure additive runtime work — backward compatible with all existing engine tests.
**Verified:** 2026-04-06T18:00:00Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `EventCompleted(target, name, duration_ns)` yielded after every event dispatch with `duration_ns` from `time.perf_counter_ns()` | ✓ VERIFIED | `engine/scheduler.py` lines 288-307: `t_start = time.perf_counter_ns()` + `yield EventCompleted(...)` in both priority and standard queue branches; `test_event_completed_emitted` PASSED |
| 2 | `ctx.event_duration_warn_ns` opt-in (default `None`); when set and exceeded, `LongEventWarning` yielded immediately after `EventCompleted` | ✓ VERIFIED | `engine/ctx.py` line 48: `self.event_duration_warn_ns: int | None = None`; `execute()` wrapper intercepts `EventCompleted` and conditionally yields `LongEventWarning`; `test_no_long_event_warning_by_default` and `test_long_event_warning_fires` PASSED |
| 3 | `SenescentEntered(instance, state, settled_at)` yielded after `TransitionFired` when new state is in `ClassManifest.senescent_states` | ✓ VERIFIED | `engine/scheduler.py` lines 463-465: yield after `TransitionFired`; final-state guard prevents emission for final states; `test_senescent_entered_on_transition` PASSED |
| 4 | `SenescentExited(instance, state, exited_at, by_event)` yielded before `EventReceived` when current state is senescent | ✓ VERIFIED | `engine/scheduler.py` lines 364-370: yield before `EventReceived` when `_senescent[ikey]` is True; `test_senescent_exited_on_wakeup` PASSED (ordering assertion confirmed) |
| 5 | `senescent_states: NotRequired[set[str]]` added to `ClassManifest` TypedDict, backward compatible | ✓ VERIFIED | `engine/manifest.py` line 31: `senescent_states: NotRequired[set[str]]`; `from typing import ... NotRequired` imported; all 42 existing engine tests pass without modification |
| 6 | Final state detection wins over senescence — entering a state that is both final and senescent emits `InstanceDeleted` and NOT `SenescentEntered` | ✓ VERIFIED | Scheduler checks `next_state not in final_states` before checking `senescent_states`; `test_final_state_wins_over_senescence` PASSED |
| 7 | No double-emission: `SenescentEntered` not re-emitted if instance already senescent | ✓ VERIFIED | `self._senescent` sidecar dict guards emission with `if not self._senescent.get(ikey, False)`; `test_no_double_senescent_entered` PASSED |
| 8 | All 42 existing engine tests pass without modification (additive only) | ✓ VERIFIED | `pytest tests/test_engine.py` — 42 passed; `pytest tests/` — 307 passed, 0 failed |
| 9 | All 10 new instrumentation tests pass (D-22.1..D-22.10) | ✓ VERIFIED | `pytest tests/test_engine_instrumentation.py -v` — 10/10 PASSED |

**Score:** 9/9 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `engine/microstep.py` | EventCompleted, LongEventWarning, SenescentEntered, SenescentExited dataclasses | ✓ VERIFIED | All 4 frozen dataclasses present at lines 136–167, correct field names, Literal type fields |
| `engine/manifest.py` | `ClassManifest.senescent_states: NotRequired[set[str]]` | ✓ VERIFIED | Line 31; `NotRequired` imported from `typing` at line 12 |
| `engine/ctx.py` | `event_duration_warn_ns` attribute + `LongEventWarning` wrapper in `execute()` | ✓ VERIFIED | Line 48: attribute init; lines 214–229: for-loop wrapper intercepts `EventCompleted` and yields `LongEventWarning` |
| `engine/__init__.py` | Re-exports for 4 new micro-step types | ✓ VERIFIED | Lines 17–20 (import block) and lines 62–65 (`__all__`) — 8 references total |
| `engine/scheduler.py` | Timing instrumentation, `_senescent` sidecar, emission helpers | ✓ VERIFIED | `import time`; `_ikey()` helper at line 50; `self._senescent` at line 82; `perf_counter_ns` at lines 288, 290, 304, 306; all emission points present |
| `tests/test_engine_instrumentation.py` | 10 test stubs all GREEN for D-22.1..D-22.10 | ✓ VERIFIED | 10/10 PASSED; zero `pytest.fail()` occurrences remaining |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `engine/scheduler.py:execute()` | `engine.microstep.EventCompleted` | `perf_counter_ns` bracket + `yield EventCompleted(...)` | ✓ WIRED | Both queue branches (priority + standard) bracket `_process_event` with timing and yield `EventCompleted` after |
| `engine/scheduler.py:_process_event` | `engine.microstep.SenescentEntered` | `cdef.get("senescent_states")` after `TransitionFired` | ✓ WIRED | Lines 461–465 check `senescent_states` and emit after transition |
| `engine/scheduler.py:_process_event` | `engine.microstep.SenescentExited` | `self._senescent.get(ikey)` before `EventReceived` | ✓ WIRED | Lines 364–370 emit before `EventReceived` when flag is True |
| `engine/ctx.py:SimulationContext.execute` | `engine.microstep.LongEventWarning` | `isinstance(step, EventCompleted)` + threshold check | ✓ WIRED | Lines 219–227: for-loop intercepts `EventCompleted`, reads `self.event_duration_warn_ns`, yields `LongEventWarning` when exceeded |
| `tests/test_engine_instrumentation.py` | `engine` | `from engine import EventCompleted, LongEventWarning, SenescentEntered, SenescentExited` | ✓ WIRED | Import confirmed; 10 tests use all 4 types in assertions |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `engine/scheduler.py:execute()` | `duration_ns` | `time.perf_counter_ns()` wall-clock measurements | Yes — real nanosecond timing, not hardcoded | ✓ FLOWING |
| `engine/ctx.py:execute()` | `threshold` | `self.event_duration_warn_ns` attribute (user-set or None) | Yes — read fresh per EventCompleted | ✓ FLOWING |
| `engine/scheduler.py:_process_event` | `sen_states` | `cdef.get("senescent_states") or set()` from ClassManifest | Yes — from manifest dict | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| All 10 instrumentation tests green | `pytest tests/test_engine_instrumentation.py -v` | 10 passed in 0.11s | ✓ PASS |
| Full test suite — no regressions | `pytest tests/ -x -q` | 307 passed in 2.41s | ✓ PASS |
| 4 new types importable from `engine` | `python -c "from engine import EventCompleted, LongEventWarning, SenescentEntered, SenescentExited; print('ok')"` | ok | ✓ PASS |
| `event_duration_warn_ns` defaults to None | `python -c "from engine import SimulationContext; ctx = SimulationContext({...}); assert ctx.event_duration_warn_ns is None"` | assertion passed | ✓ PASS |
| Frozen dataclasses raise on mutation | Python assertion: `FrozenInstanceError` raised on field write | FrozenInstanceError raised | ✓ PASS |
| SC-11 — engine has zero schema/tools/pycca imports | `python -c "import engine; print('ok')"` | ok | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| MCP-08 (partial — runtime instrumentation) | All 3 plans | Simulation engine runtime observability | ✓ SATISFIED | All 9 roadmap SC for 5.1.1 verified; 307 tests green |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| None found | — | — | — | — |

No `pytest.fail()` stubs remain in `tests/test_engine_instrumentation.py` (grep count: 0). No TODO/FIXME/placeholder comments in modified engine files. No hardcoded empty returns in the instrumentation paths.

### Human Verification Required

None. All success criteria are verifiable programmatically via test suite execution and code inspection.

### Gaps Summary

No gaps. All 9 observable truths verified, all artifacts substantive and wired, data flows confirmed, 307 tests passing with 0 failures.

**Note on SC-3 wording:** The roadmap SC-3 references `StateEntered` as the emission trigger for `SenescentEntered`. `StateEntered` does not exist as a micro-step type in the engine — the CONTEXT.md and all plan files specify `TransitionFired` as the correct trigger point. The implementation correctly emits `SenescentEntered` after `TransitionFired`, which is the actual design decision. The roadmap SC wording was informal shorthand, not a type name. This is not a gap.

---

_Verified: 2026-04-06T18:00:00Z_
_Verifier: Claude (gsd-verifier)_
